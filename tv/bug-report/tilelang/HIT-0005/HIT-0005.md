# HIT-0005: tl.enable_fast_math on cap_82592c2e5e1f5133
Verdict: CONFIRMED
Arch:    generic
Line:    tilelang (TileLang 0.1.12, pypi wheel, H100 sm_90a, nvcc 12.8.93)

Summary: two compilations of the same TileLang PrimFunc, differing only in the
pass set, produce different output bits on the same input.  Reference is the
stock CUDA pipeline; the candidate differs by: setting pass_configs {"tl.enable_fast_math": true}.
5627 of 2107392 elements differ,
max abs difference 0.00390625, max 9 ulp.

Labels: pass-class:numerics-changing, kernel-has-gemm, kernel-has-reduce, input-mode:captured, exact-int-accum:reassociation-excluded, gross

## Reproduction manual

```bash
source /home/youngzt/fuzz-tilelang/env.sh
export PYTHONPATH=/home/youngzt/fuzz-tilelang
cd /home/youngzt/fuzz-tilelang
python -m tlfz.repro cap_82592c2e5e1f5133 '{"skip": [], "cfg": {"tl.enable_fast_math": true}}' --mode captured --seed 267
```

Expected output: `BIT DIFFERENCE` with the numbers above.  The script compiles
the kernel twice (stock pipeline, then the stock pipeline minus the pass under
test), fills every tensor parameter -- inputs *and* outputs -- with identical
deterministic content on both sides, runs both, and compares every parameter
bit for bit.

`TILELANG_DISABLE_CACHE=1` is mandatory and is set by `env.sh`: TileLang's
kernel cache key does not cover a re-registered pass pipeline, so with the
cache on you get the previous variant's binary back.

## Minimal kernel source

corpus id `cap_82592c2e5e1f5133`, provenance `examples/deepseek_mhc/test_example_mhc.py`

```python
# from tvm.script import tirx as T

@T.prim_func
def mhc_pre_big_fuse_tilelang(gemm_out_mul: T.handle, gemm_out_sqrsum: T.handle, hc_scale: T.handle, hc_base: T.handle, residual: T.handle, post_mix: T.handle, comb_mix: T.handle, layer_input: T.handle):
    num_tokens = T.int32()
    gemm_out_mul_1 = T.match_buffer(gemm_out_mul, (1, num_tokens, 24), strides=(24 * num_tokens, 24, 1))
    gemm_out_sqrsum_1 = T.match_buffer(gemm_out_sqrsum, (1, num_tokens), strides=(num_tokens, 1))
    hc_scale_1 = T.match_buffer(hc_scale, (3,), strides=(1,))
    hc_base_1 = T.match_buffer(hc_base, (24,), strides=(1,))
    residual_1 = T.match_buffer(residual, (num_tokens, 4, 4096), "bfloat16", strides=(16384, 4096, 1))
    post_mix_1 = T.match_buffer(post_mix, (num_tokens, 4), strides=(4, 1))
    comb_mix_1 = T.match_buffer(comb_mix, (num_tokens, 16), strides=(16, 1))
    layer_input_1 = T.match_buffer(layer_input, (num_tokens, 4096), "bfloat16", strides=(4096, 1))
    # with T.sblock("root"):
    for bx in T.thread_binding(num_tokens, thread="blockIdx.x"):
        for tx in T.thread_binding(96, thread="threadIdx.x"):
            for ty in T.thread_binding(1, thread="threadIdx.y"):
                for tz in T.thread_binding(1, thread="threadIdx.z"):
                    with T.sblock("tilelang_root"):
                        T.reads()
                        T.writes()
                        rms = T.sblock_alloc_buffer((1,), scope="local.fragment")
                        mixes = T.sblock_alloc_buffer((24,), scope="local.fragment")
                        mixes_shared = T.sblock_alloc_buffer((24,), scope="shared.dyn")
                        cm = T.sblock_alloc_buffer((4, 4), scope="local.fragment")
                        row_sum = T.sblock_alloc_buffer((4,), scope="local.fragment")
                        col_sum = T.sblock_alloc_buffer((4,), scope="local.fragment")
                        row_max = T.sblock_alloc_buffer((4,), scope="local.fragment")
                        pre_mix_shared = T.sblock_alloc_buffer((4,), scope="shared.dyn")
                        xs = T.sblock_alloc_buffer((4, 512), scope="shared.dyn")
                        xl = T.sblock_alloc_buffer((4, 512), scope="local.fragment")
                        ol = T.sblock_alloc_buffer((512,), scope="local.fragment")
                        T.fill(T.region(mixes[0], 2, 24), 0)
                        rms[0] = T.float32(0.0)
                        for i_split in range(1):
                            rms[0] = rms[0] + gemm_out_sqrsum_1[i_split, bx]
                        rms[0] = T.rsqrt(rms[0] / T.float32(16384.0) + T.float32(9.9999999999999995e-07))
                        for j in T.parallel(24):
                            mixes[j] = T.float32(0.0)
                            for i_split in range(1):
                                mixes[j] = mixes[j] + gemm_out_mul_1[i_split, bx, j]
                            mixes[j] = mixes[j] * rms[0]
                        T.copy(T.region(mixes[0], 1, 24), T.region(mixes_shared[0], 2, 24))
                        if tx < 32:
                            for j in T.parallel(4):
                                post_mix_1[bx, j] = T.sigmoid(mixes_shared[j + 4] * hc_scale_1[1] + hc_base_1[j + 4])
                            for j in T.parallel(4):
                                for k in T.parallel(4):
                                    cm[j, k] = mixes_shared[j * 4 + k + 8] * hc_scale_1[2] + hc_base_1[j * 4 + k + 8]
                            T.reduce(T.region(cm[0, 0], 1, 4, 4), T.region(row_max[0], 2, 4), "max", 1, T.bool(True))
                            for j in T.parallel(4):
                                for k in T.parallel(4):
                                    cm[j, k] = T.exp(cm[j, k] - row_max[j])
                            T.reduce(T.region(cm[0, 0], 1, 4, 4), T.region(row_sum[0], 2, 4), "sum", 1, T.bool(True))
                            for j in T.parallel(4):
                                for k in T.parallel(4):
                                    cm[j, k] = cm[j, k] / row_sum[j] + T.float32(9.9999999999999995e-07)
                            T.reduce(T.region(cm[0, 0], 1, 4, 4), T.region(col_sum[0], 2, 4), "sum", 0, T.bool(True))
                            for j in T.parallel(4):
                                for k in T.parallel(4):
                                    cm[j, k] = cm[j, k] / (col_sum[k] + T.float32(9.9999999999999995e-07))
                            for _ in range(9):
                                T.reduce(T.region(cm[0, 0], 1, 4, 4), T.region(row_sum[0], 2, 4), "sum", 1, T.bool(True))
                                for j in T.parallel(4):
                                    for k in T.parallel(4):
                                        cm[j, k] = cm[j, k] / (row_sum[j] + T.float32(9.9999999999999995e-07))
                                T.reduce(T.region(cm[0, 0], 1, 4, 4), T.region(col_sum[0], 2, 4), "sum", 0, T.bool(True))
                                for j in T.parallel(4):
                                    for k in T.parallel(4):
                                        cm[j, k] = cm[j, k] / (col_sum[k] + T.float32(9.9999999999999995e-07))
                            for j in T.parallel(4):
                                for k in T.parallel(4):
                                    comb_mix_1[bx, j * 4 + k] = cm[j, k]
                        else:
                            for j in T.parallel(4):
                                pre_mix_shared[j] = T.sigmoid(mixes_shared[j] * hc_scale_1[0] + hc_base_1[j]) + T.float32(9.9999999999999995e-07)
                            for i0_h in T.serial(8, annotations={"num_stages": 2}):
                                T.copy(T.region(residual_1[bx, 0, i0_h * 512], 1, 1, 4, 512), T.region(xs[0, 0], 2, 4, 512))
                                T.copy(T.region(xs[0, 0], 1, 4, 512), T.region(xl[0, 0], 2, 4, 512))
                                T.fill(T.region(ol[0], 2, 512), 0)
                                for i_hc in range(4):
                                    pre: T.float32 = pre_mix_shared[i_hc]
                                    for i1_h in T.parallel(512):
                                        ol[i1_h] = ol[i1_h] + pre * xl[i_hc, i1_h]
                                T.copy(T.region(ol[0], 1, 512), T.region(layer_input_1[bx, i0_h * 512], 2, 1, 512))
```

## Pass pipeline: reference vs candidate

Reference: `tilelang/cuda/pipeline.py::CUDAPassPipelineBody`, reproduced step by
step in `tlfz/steps.py` (verified byte-identical CUDA output with an empty skip
set before every run).

Candidate: the same list with **(none)** removed and pass_configs {"tl.enable_fast_math": true}.

Variant as first found: `{"skip": [], "cfg": {"tl.enable_fast_math": true}}`
Minimal variant after bisection: `{"skip": [], "cfg": {"tl.enable_fast_math": true}}`

## IR: input -> output

`ir/input.tir.py` is the tile-level IR going in.  `ir/ref_after_whole_pipeline.tir.py`
and `ir/cand_after_whole_pipeline.tir.py` are what comes out on the
two paths -- for a dropped pass the bracket is that one step, for a pass_config
it is the whole pipeline (captured in process; TileLang IR dumps do not
re-parse, so there is no file round-trip).  `ir/reference.cu` vs `ir/candidate.cu` is the generated CUDA on both
paths (11705 vs 11665 chars).

## Inputs

Generated, not stored: input mode `captured`, seed `267`,
regenerated by `tlfz.core.make_inputs`.  Input modes that also reproduce it:
['intvalued', 'normal', 'uniform']

## Observed

first differing element: {"idx": 2, "ref": 0.4744965434074402, "cand": 0.4744965136051178}
per-parameter difference records: [{"arg": 5, "dtype": "float32", "shape": [512, 4], "kind": "float", "nan_ref": 0, "nan_cand": 0, "nan_mask_differs": false, "n_diff": 378, "n_total": 2048, "max_abs": 5.960464477539063e-08, "max_rel": 1.1058769899818799e-07, "ulp_max": 1, "first": {"idx": 2, "ref": 0.4744965434074402, "cand": 0.4744965136051178}}, {"arg": 6, "dtype": "float32", "shape": [512, 16], "kind": "float", "nan_ref": 0, "nan_cand": 0, "nan_mask_differs": false, "n_diff": 5189, "n_total": 8192, "max_abs": 1.1920928955078125e-07, "max_rel": 4.588354158840957e-07, "ulp_max": 5, "first": {"idx": 1, "ref": 0.2740423381328583, "cand": 0.2740423083305359}}]

## Determinism evidence

reference self-consistent across repeats: True
candidate self-consistent across repeats: True
kernel passed the pre-screen (same build twice, bit-identical) before any
variant ran, and the reference was also re-run with a 17 MB heap shift to rule
out an out-of-bounds read: difference still present under the heap shift:
True

## Exact-integer control

integer-valued inputs, fp32 accumulator provably exact (max |acc| < 2^24):
control applies = True, difference still present under
it = True.  When both are true, summation order
cannot explain the difference.

## SMT reachability analysis

TODO: written by a reviewing agent.

Raw material for that reviewer:
- culprit pass: `None`; pass class: numerics-changing
- pass_configs in the minimal variant: {"tl.enable_fast_math": true}
- kernel contains a tile GEMM: True; a tile reduction: True; an atomic: False
- difference is deterministic on both sides: ref=True, cand=True
- concurrency / cross-instance constructs present in the generated code:
  wgmma=False, mbarrier=False,
  TMA=False,
  __syncthreads=True, atomics=False,
  warp specialisation=False
- exact-integer control applies=True, still differs=True
- element count differing: 5627 / 2107392; max ulp 9
