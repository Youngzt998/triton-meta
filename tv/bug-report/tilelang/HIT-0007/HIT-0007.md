# HIT-0007: PipelinePlanning on cap_c84d73cfbb167409
Verdict: CONFIRMED
Arch:    hopper-only
Line:    tilelang (TileLang 0.1.12, pypi wheel, H100 sm_90a, nvcc 12.8.93)

Summary: two compilations of the same TileLang PrimFunc, differing only in the
pass set, produce different output bits on the same input.  Reference is the
stock CUDA pipeline; the candidate differs by: dropping PipelinePlanning.
16 of 524288 elements differ,
max abs difference 0.001953125, max 1 ulp.

Labels: pass-class:bit-preserving, layout-reorders-reduction, kernel-has-gemm, kernel-has-reduce, input-mode:captured, exact-int-accum:reassociation-excluded, few-ulp

## Reproduction manual

```bash
source /home/youngzt/fuzz-tilelang/env.sh
export PYTHONPATH=/home/youngzt/fuzz-tilelang
cd /home/youngzt/fuzz-tilelang
python -m tlfz.repro cap_c84d73cfbb167409 '{"skip": ["PipelinePlanning"], "cfg": {}}' --mode captured --seed 403
```

Expected output: `BIT DIFFERENCE` with the numbers above.  The script compiles
the kernel twice (stock pipeline, then the stock pipeline minus the pass under
test), fills every tensor parameter -- inputs *and* outputs -- with identical
deterministic content on both sides, runs both, and compares every parameter
bit for bit.

`TILELANG_DISABLE_CACHE=1` is mandatory and is set by `env.sh`: TileLang's
kernel cache key does not cover a re-registered pass pipeline, so with the
cache on you get the previous variant's binary back.

## Minimal kernel source

corpus id `cap_c84d73cfbb167409`, provenance `examples/deepseek_v4/test_tilelang_example_deepseek_v4.py`

```python
# from tvm.script import tirx as T

@T.prim_func
def main(Q_handle: T.handle, KV_handle: T.handle, TopkIndices_handle: T.handle, Output_handle: T.handle, Sinks_handle: T.handle):
    Q = T.match_buffer(Q_handle, (1, 512, 8, 128), "bfloat16", strides=(524288, 1024, 128, 1))
    KV = T.match_buffer(KV_handle, (1, 1024, 128), "bfloat16", strides=(131072, 128, 1))
    TopkIndices = T.match_buffer(TopkIndices_handle, (1, 512, 256), "int32", strides=(131072, 256, 1))
    Output = T.match_buffer(Output_handle, (1, 512, 8, 128), "bfloat16", strides=(524288, 1024, 128, 1))
    Sinks = T.match_buffer(Sinks_handle, (8,), "bfloat16", strides=(1,))
    # with T.sblock("root"):
    for bx in T.thread_binding(1, thread="blockIdx.x"):
        for by in T.thread_binding(512, thread="blockIdx.y"):
            for bz in T.thread_binding(1, thread="blockIdx.z"):
                for tx in T.thread_binding(256, thread="threadIdx.x"):
                    for ty in T.thread_binding(1, thread="threadIdx.y"):
                        for tz in T.thread_binding(1, thread="threadIdx.z"):
                            with T.sblock("tilelang_root"):
                                T.reads()
                                T.writes()
                                Q_shared = T.sblock_alloc_buffer((64, 128), "bfloat16", scope="shared.dyn")
                                KV_shared = T.sblock_alloc_buffer((64, 128), "bfloat16", scope="shared.dyn")
                                S_shared = T.sblock_alloc_buffer((64, 64), "bfloat16", scope="shared.dyn")
                                Sinks_shared = T.sblock_alloc_buffer((64,), "bfloat16", scope="shared.dyn")
                                acc_s = T.sblock_alloc_buffer((64, 64), scope="local.fragment")
                                acc_o = T.sblock_alloc_buffer((64, 128), scope="local.fragment")
                                acc_o_shared = T.sblock_alloc_buffer((64, 128), "bfloat16", scope="shared.dyn")
                                scores_max = T.sblock_alloc_buffer((64,), scope="local.fragment")
                                scores_max_prev = T.sblock_alloc_buffer((64,), scope="local.fragment")
                                scores_scale = T.sblock_alloc_buffer((64,), scope="local.fragment")
                                scores_sum = T.sblock_alloc_buffer((64,), scope="local.fragment")
                                logsum = T.sblock_alloc_buffer((64,), scope="local.fragment")
                                mask = T.sblock_alloc_buffer((64,), "bool", scope="local.fragment")
                                h_start: T.int32 = bx * 64
                                T.copy(T.region(Q[bz, by, h_start, 0], 1, 1, 1, 64, 128), T.region(Q_shared[0, 0], 2, 64, 128))
                                T.copy(T.region(Sinks[h_start], 1, 64), T.region(Sinks_shared[0], 2, 64))
                                T.fill(T.region(acc_o[0, 0], 2, 64, 128), 0)
                                T.fill(T.region(logsum[0], 2, 64), 0)
                                T.fill(T.region(scores_max[0], 2, 64), -1073741824)
                                for i_i in T.serial(4, annotations={"num_stages": 2}):
                                    for bi_i in T.parallel(64):
                                        idx: T.int32 = TopkIndices[bz, by, i_i * 64 + bi_i]
                                        mask[bi_i] = idx >= 0
                                    for bi_i in T.parallel(64):
                                        for d_i in T.parallel(128):
                                            idx: T.int32 = TopkIndices[bz, by, i_i * 64 + bi_i]
                                            KV_shared[bi_i, d_i] = KV[bz, idx, d_i]
                                    for h_i in T.parallel(64):
                                        for bi_i in T.parallel(64):
                                            acc_s[h_i, bi_i] = T.if_then_else(mask[bi_i], T.float32(0.0), T.infinity("float32") * T.float32(-1.0))
                                    T.gemm(T.region(Q_shared[0, 0], 1, 64, 128), T.region(KV_shared[0, 0], 1, 64, 128), T.region(acc_s[0, 0], 3, 64, 64), T.bool(False), T.bool(True), 64, 64, 128, 1, T.bool(False), 128, 128, 0, 0, 1, 0, 0, 0, 0)
                                    T.copy(T.region(scores_max[0], 1, 64), T.region(scores_max_prev[0], 2, 64))
                                    T.reduce(T.region(acc_s[0, 0], 1, 64, 64), T.region(scores_max[0], 2, 64), "max", 1, T.bool(False))
                                    for h_i in T.parallel(64):
                                        scores_max[h_i] = T.max(scores_max[h_i], scores_max_prev[h_i])
                                    for h_i in T.parallel(64):
                                        scores_scale[h_i] = T.exp2(scores_max_prev[h_i] * T.float32(0.1275174307460247) - scores_max[h_i] * T.float32(0.1275174307460247))
                                    for h_i in T.parallel(64):
                                        for bi_i in T.parallel(64):
                                            acc_s[h_i, bi_i] = T.exp2(acc_s[h_i, bi_i] * T.float32(0.1275174307460247) - scores_max[h_i] * T.float32(0.1275174307460247))
                                    T.reduce(T.region(acc_s[0, 0], 1, 64, 64), T.region(scores_sum[0], 2, 64), "sum", 1, T.bool(True))
                                    for h_i in T.parallel(64):
                                        for d_i in T.parallel(128):
                                            acc_o[h_i, d_i] = acc_o[h_i, d_i] * scores_scale[h_i]
                                    T.copy(T.region(acc_s[0, 0], 1, 64, 64), T.region(S_shared[0, 0], 2, 64, 64))
                                    T.gemm(T.region(S_shared[0, 0], 1, 64, 64), T.region(KV_shared[0, 0], 1, 64, 128), T.region(acc_o[0, 0], 3, 64, 128), T.bool(False), T.bool(False), 64, 128, 64, 1, T.bool(False), 64, 128, 0, 0, 1, 0, 0, 0, 0)
                                    for h_i in T.parallel(64):
                                        logsum[h_i] = logsum[h_i] * scores_scale[h_i] + scores_sum[h_i]
                                for h_i in T.parallel(64):
                                    logsum[h_i] = logsum[h_i] + T.exp2(T.Cast("float32", Sinks_shared[h_i]) * T.float32(1.44269504) - scores_max[h_i] * T.float32(0.1275174307460247))
                                for h_i in T.parallel(64):
                                    for d_i in T.parallel(128):
                                        acc_o[h_i, d_i] = acc_o[h_i, d_i] / logsum[h_i]
                                T.copy(T.region(acc_o[0, 0], 1, 64, 128), T.region(acc_o_shared[0, 0], 2, 64, 128))
                                T.copy(T.region(acc_o_shared[0, 0], 1, 64, 128), T.region(Output[bz, by, h_start, 0], 2, 1, 1, 64, 128))
```

## Pass pipeline: reference vs candidate

Reference: `tilelang/cuda/pipeline.py::CUDAPassPipelineBody`, reproduced step by
step in `tlfz/steps.py` (verified byte-identical CUDA output with an empty skip
set before every run).

Candidate: the same list with **PipelinePlanning** removed.

Variant as first found: `{"skip": ["PipelinePlanning"], "cfg": {}}`
Minimal variant after bisection: `{"skip": ["PipelinePlanning"], "cfg": {}}`

## IR: input -> output

`ir/input.tir.py` is the tile-level IR going in.  `ir/ref_after_PipelinePlanning.tir.py`
and `ir/cand_after_PipelinePlanning.tir.py` are what comes out on the
two paths -- for a dropped pass the bracket is that one step, for a pass_config
it is the whole pipeline (captured in process; TileLang IR dumps do not
re-parse, so there is no file round-trip).  `ir/reference.cu` vs `ir/candidate.cu` is the generated CUDA on both
paths (25258 vs 12237 chars).

## Inputs

Generated, not stored: input mode `captured`, seed `403`,
regenerated by `tlfz.core.make_inputs`.  Input modes that also reproduce it:
['intvalued', 'normal', 'uniform']

## Observed

first differing element: {"idx": 24537, "ref": 0.000278472900390625, "cand": 0.0002803802490234375}
per-parameter difference records: [{"arg": 3, "dtype": "bfloat16", "shape": [1, 512, 8, 128], "kind": "float", "nan_ref": 0, "nan_cand": 0, "nan_mask_differs": false, "n_diff": 16, "n_total": 524288, "max_abs": 0.001953125, "max_rel": 0.0069444444961845875, "ulp_max": 1, "first": {"idx": 24537, "ref": 0.000278472900390625, "cand": 0.0002803802490234375}}]

## Determinism evidence

reference self-consistent across repeats: True
candidate self-consistent across repeats: True
kernel passed the pre-screen (same build twice, bit-identical) before any
variant ran, and the reference was also re-run with a 17 MB heap shift to rule
out an out-of-bounds read: difference still present under the heap shift:
True

## Exact-integer control

integer-valued inputs, fp32 accumulator provably exact (max |acc| < 2^24):
control applies = True, difference still present under
it = True.  When both are true, summation order
cannot explain the difference.

## SMT reachability analysis

TODO: written by a reviewing agent.

Raw material for that reviewer:
- culprit pass: `PipelinePlanning`; pass class: bit-preserving
- pass_configs in the minimal variant: (none)
- kernel contains a tile GEMM: True; a tile reduction: True; an atomic: False
- difference is deterministic on both sides: ref=True, cand=True
- concurrency / cross-instance constructs present in the generated code:
  wgmma=True, mbarrier=False,
  TMA=True,
  __syncthreads=True, atomics=False,
  warp specialisation=False
- exact-integer control applies=True, still differs=True
- element count differing: 16 / 524288; max ulp 1
