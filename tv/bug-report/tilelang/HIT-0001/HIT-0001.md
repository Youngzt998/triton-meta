# HIT-0001: ProducerConsumerWarpSpecialized on hw_fa_1x1x128x256x64_c0_s2
Verdict: CONFIRMED
Arch:    hopper-only
Line:    tilelang (TileLang 0.1.12, pypi wheel, H100 sm_90a, nvcc 12.8.93)

Summary: two compilations of the same TileLang PrimFunc, differing only in the
pass set, produce different output bits on the same input.  Reference is the
stock CUDA pipeline; the candidate is the stock pipeline with
`ProducerConsumerWarpSpecialized` dropped and pass_configs `(none)`.
2 of 8192 elements differ,
max abs difference 1.52587890625e-05, max 1 ulp.

Labels: pass-class:numerics-changing, layout-reorders-reduction, kernel-has-gemm, kernel-has-reduce, input-mode:normal, exact-int-accum:reassociation-excluded, few-ulp

## Reproduction manual

```bash
source /home/youngzt/fuzz-tilelang/env.sh
export PYTHONPATH=/home/youngzt/fuzz-tilelang
cd /home/youngzt/fuzz-tilelang
python -m tlfz.repro hw_fa_1x1x128x256x64_c0_s2 '{"skip": ["ProducerConsumerWarpSpecialized"], "cfg": {}}' --mode normal --seed 26
```

Expected output: `BIT DIFFERENCE` with the numbers above.  The script compiles
the kernel twice (stock pipeline, then the stock pipeline minus the pass under
test), fills every tensor parameter -- inputs *and* outputs -- with identical
deterministic content on both sides, runs both, and compares every parameter
bit for bit.

`TILELANG_DISABLE_CACHE=1` is mandatory and is set by `env.sh`: TileLang's
kernel cache key does not cover a re-registered pass pipeline, so with the
cache on you get the previous variant's binary back.

## Minimal kernel source

corpus id `hw_fa_1x1x128x256x64_c0_s2`, provenance `tlfz.mkcorpus:fa_1x1x128x256x64_c0_s2`

```python
import tilelang
import tilelang.language as T


@T.prim_func
def main(Q: T.Tensor((1, 1, 128, 64), "float16"),
         K: T.Tensor((1, 1, 256, 64), "float16"),
         V: T.Tensor((1, 1, 256, 64), "float16"),
         O: T.Tensor((1, 1, 128, 64), "float16")):
    with T.Kernel(T.ceildiv(128, 64), 1, 1, threads=128) as (bx, by, bz):
        Qs = T.alloc_shared([64, 64], "float16")
        Ks = T.alloc_shared([64, 64], "float16")
        Vs = T.alloc_shared([64, 64], "float16")
        Os = T.alloc_shared([64, 64], "float16")
        s = T.alloc_fragment([64, 64], "float32")
        sc = T.alloc_fragment([64, 64], "float16")
        o = T.alloc_fragment([64, 64], "float32")
        mx = T.alloc_fragment([64], "float32")
        mp = T.alloc_fragment([64], "float32")
        ss = T.alloc_fragment([64], "float32")
        sm = T.alloc_fragment([64], "float32")
        ls = T.alloc_fragment([64], "float32")
        T.copy(Q[bz, by, bx * 64:(bx + 1) * 64, :], Qs)
        T.fill(o, 0)
        T.fill(ls, 0)
        T.fill(mx, -T.infinity("float32"))
        for k in T.Pipelined(T.ceildiv(256, 64), num_stages=2):
            T.copy(K[bz, by, k * 64:(k + 1) * 64, :], Ks)
            T.clear(s)
            T.gemm(Qs, Ks, s, transpose_B=True, policy=T.GemmWarpPolicy.FullRow)
            T.copy(V[bz, by, k * 64:(k + 1) * 64, :], Vs)
            T.copy(mx, mp)
            T.fill(mx, -T.infinity("float32"))
            T.reduce_max(s, mx, dim=1, clear=False)
            for i in T.Parallel(64):
                ss[i] = T.exp2(mp[i] * 0.18033688 - mx[i] * 0.18033688)
            for i, j in T.Parallel(64, 64):
                o[i, j] *= ss[i]
            for i, j in T.Parallel(64, 64):
                s[i, j] = T.exp2(s[i, j] * 0.18033688 - mx[i] * 0.18033688)
            T.copy(s, sc)
            T.gemm(sc, Vs, o, policy=T.GemmWarpPolicy.FullRow)
            T.reduce_sum(s, sm, dim=1)
            for i in T.Parallel(64):
                ls[i] = ls[i] * ss[i] + sm[i]
        for i, j in T.Parallel(64, 64):
            o[i, j] /= ls[i]
        T.copy(o, Os)
        T.copy(Os, O[bz, by, bx * 64:(bx + 1) * 64, :])

```

## Pass pipeline: reference vs candidate

Reference: `tilelang/cuda/pipeline.py::CUDAPassPipelineBody`, reproduced step by
step in `tlfz/steps.py` (verified byte-identical CUDA output with an empty skip
set before every run).

Candidate: the same list with **ProducerConsumerWarpSpecialized** removed.

Variant as first found: `{"skip": ["ProducerConsumerWarpSpecialized"], "cfg": {}}`
Minimal variant after bisection: `{"skip": ["ProducerConsumerWarpSpecialized"], "cfg": {}}`

## IR: input -> output

`ir/ref_before_ProducerConsumerWarpSpecialized.tir.py` -> `ir/ref_after_ProducerConsumerWarpSpecialized.tir.py` is what the
culprit pass does on the reference path (captured in process; TileLang IR dumps
do not re-parse, so there is no file round-trip).  The candidate simply skips
that step.  `ir/reference.cu` vs `ir/candidate.cu` is the generated CUDA on both
paths (8764 vs 18130 chars).

## Inputs

Generated, not stored: input mode `normal`, seed `26`,
regenerated by `tlfz.core.make_inputs`.  Input modes that also reproduce it:
['intvalued', 'normal']

## Observed

first differing element: {"idx": 1138, "ref": -0.0187835693359375, "cand": -0.018798828125}
per-parameter difference records: [{"arg": 3, "dtype": "float16", "shape": [1, 1, 128, 64], "kind": "float", "nan_ref": 0, "nan_cand": 0, "nan_mask_differs": false, "n_diff": 2, "n_total": 8192, "max_abs": 1.52587890625e-05, "max_rel": 0.0009727626456879079, "ulp_max": 1, "first": {"idx": 1138, "ref": -0.0187835693359375, "cand": -0.018798828125}}]

## Determinism evidence

reference self-consistent across repeats: True
candidate self-consistent across repeats: True
kernel passed the pre-screen (same build twice, bit-identical) before any
variant ran, and the reference was also re-run with a 17 MB heap shift to rule
out an out-of-bounds read: difference still present under the heap shift:
True

## Exact-integer control

integer-valued inputs, fp32 accumulator provably exact (max |acc| < 2^24):
control applies = True, difference still present under
it = True.  When both are true, summation order
cannot explain the difference.

## SMT reachability analysis

TODO: written by a reviewing agent.

Raw material for that reviewer:
- culprit pass: `ProducerConsumerWarpSpecialized`; pass class: numerics-changing
- pass_configs in the minimal variant: (none)
- kernel contains a tile GEMM: True; a tile reduction: True; an atomic: False
- difference is deterministic on both sides: ref=True, cand=True
- concurrency / cross-instance constructs present in the generated code:
  wgmma=True, mbarrier=True,
  TMA=True,
  __syncthreads=True, atomics=False,
  warp specialisation=False
- exact-integer control applies=True, still differs=True
- element count differing: 2 / 8192; max ulp 1
