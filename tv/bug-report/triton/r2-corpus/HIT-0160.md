# HIT-0160: triton-loop-aware-cse on ind:triton_red_fused_prepare_softmax_online_0_110dfea7 [ttgir]
Verdict: CONFIRMED
Arch:    generic
Line:    r2-corpus
Level:   ttgir
Culprit pass: `--triton-loop-aware-cse + --tritongpu-coalesce + --tritongpu-remove-layout-conversions + --triton-nvidia-optimize-descriptor-encoding`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: compiling `ind:triton_red_fused_prepare_softmax_online_0_110dfea7` from the corpus at the shape and compile config
below, the pipeline shown and the same pipeline plus one extra pass produce
cubins whose outputs differ bit for bit on the same input. That is the whole
claim: a bit difference between two compilations. No judgement about whether it
is a compiler bug is made here -- the labels below carry the material a
reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Shape and config draw (round 2)

Round 1 pinned both of these; round 2 draws them per round, identically for
both arms, so they are part of the experiment's setting and never part of the
control.

* config: `w4 cta1 st8 reg128 bf16x3`
* shape : `grid=(1, 1, 1)  consts=R0_BLOCK=128`

```json
{
 "num_warps": 4,
 "num_ctas": 1,
 "num_stages": 8,
 "maxnreg": 128,
 "default_dot_input_precision": "bf16x3",
 "enable_fp_fusion": false,
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": true
}
```

```json
{
 "align": {},
 "consts": {
  "R0_BLOCK": 128
 },
 "scalars": {},
 "grid": [
  1,
  1,
  1
 ]
}
```

Pointer alignment note: when a base pointer offset is non-zero, the
`tt.divisibility` argument attribute is set to match the address the launcher
really passes (the same rule `JITFunction.run` uses). The compiler is never
told an alignment it does not get, so an unaligned draw is not undefined
behaviour.

## Labels

* pass-class: layout
* level: ttgir
* config: w4 cta1 st8 reg128 bf16x3
* shape draw: grid=(1, 1, 1)  consts=R0_BLOCK=128
* layout-reorders-reduction
* determinism at this shape/config: ref 4/4, cand 4/4 identical runs of the same binary
* exact-integer control (every input a small whole number, so a plain fp32 sum is exact): goes equal
* all-inputs-equal control (every float input the same number, so a mean / variance / Welford accumulator is exact too): goes equal, but whether it ran meaningfully could NOT be established, so no conclusion is drawn from it: no 4-byte word changed under an all-equal fill
* no reassociation claim: a difference must survive BOTH controls before a reordered fp32 sum can be ruled out.
* reassociation_excluded: false -- no claim is made; a difference must survive BOTH controls before a reordered fp32 sum can be ruled out
* onesish_control: UNINFORMATIVE: equal, but the coverage probe is UNDECIDED (no 4-byte word changed under an all-equal fill), so nothing is concluded from it
* ndiff/total: 90/1048576
* of those, real value differences: 0; NaN-payload-only: 90
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* exact-integer control (every input a small whole number, so a plain fp32 sum is exact): goes equal
* all-inputs-equal control (every float input the same number, so a mean / variance / Welford accumulator is exact too): goes equal, but whether it ran meaningfully could NOT be established, so no conclusion is drawn from it: no 4-byte word changed under an all-equal fill
* no reassociation claim: a difference must survive BOTH controls before a reordered fp32 sum can be ruled out.
* determinism of each arm at this shape/config: reference 4/4 identical
  runs, candidate 4/4 identical runs of the same binary on one input

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-corpus/env.sh
cd /home/youngzt/fuzz/r2-corpus
python -m fz.repro --hit HIT-0160
```

That script rebuilds both sides from the corpus at the recorded shape and
config and reruns the comparison. To do the pass step by hand:

```bash
source /home/youngzt/fuzz/r2-corpus/env.sh
cd /home/youngzt/fuzz/r2-corpus
# reference IR (already saved):  artifacts/HIT-0160/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0160/output.ttgir
triton-opt artifacts/HIT-0160/input.ttgir --triton-loop-aware-cse --tritongpu-coalesce --tritongpu-remove-layout-conversions --triton-nvidia-optimize-descriptor-encoding -o /tmp/HIT-0160.ttgir
diff artifacts/HIT-0160/output.ttgir /tmp/HIT-0160.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Minimal kernel source

Corpus `inductor`, kernel `ind:triton_red_fused_prepare_softmax_online_0_110dfea7`, launch grid `(1, 1, 1)`.
Argument spec (after the shape draw is applied):

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null,
  "shape": null,
  "dtype": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null,
  "shape": null,
  "dtype": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 128,
  "shape": null,
  "dtype": null
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 100,
  "shape": null,
  "dtype": null
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 128,
  "shape": null,
  "dtype": null
 },
 {
  "name": "R0_BLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 64,
  "shape": null,
  "dtype": null
 }
]
```

## Pass pipeline: reference vs candidate

Level: `ttgir`. At TTIR level both arms then get the identical
`convert-triton-to-tritongpu` and the identical TTGIR base pipeline
`(none)`, so the only difference between the arms is still exactly the
pass set below.

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--triton-loop-aware-cse \
      --tritongpu-coalesce \
      --tritongpu-remove-layout-conversions \
      --triton-nvidia-optimize-descriptor-encoding
```

## IR

* `artifacts/HIT-0160/root.ttgir`   -- the root IR at this shape and config
* `artifacts/HIT-0160/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0160/output.ttgir` -- after the reference pipeline + `--triton-loop-aware-cse + --tritongpu-coalesce + --tritongpu-remove-layout-conversions + --triton-nvidia-optimize-descriptor-encoding`
* `artifacts/HIT-0160/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [4, 1], threadsPerWarp = [32, 1], warpsPerCTA = [1, 4], order = [0, 1]}>
@@ -3,3 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [4], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
@@ -8,7 +6,2 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<128x128xf32, #blocked>
-    %cst_0 = arith.constant dense<6400> : tensor<128x1xi32, #blocked1>
-    %cst_1 = arith.constant dense<64> : tensor<1x128xi32, #blocked>
-    %cst_2 = arith.constant dense<100> : tensor<1x128xi32, #blocked>
-    %cst_3 = arith.constant dense<0xFF800000> : tensor<128x128xf32, #blocked>
-    %cst_4 = arith.constant dense<64> : tensor<128x1xi32, #blocked1>
-    %cst_5 = arith.constant dense<128> : tensor<128x1xi32, #blocked1>
+    %cst = arith.constant dense<0xFF800000> : tensor<128x128xf32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<128x128xf32, #blocked>
@@ -15,0 +9,6 @@
+    %cst_1 = arith.constant dense<128> : tensor<128x1xi32, #blocked1>
+    %cst_2 = arith.constant dense<128> : tensor<128x1xi32, #blocked>
+    %cst_3 = arith.constant dense<64> : tensor<128x1xi32, #blocked>
+    %cst_4 = arith.constant dense<6400> : tensor<128x1xi32, #blocked>
+    %cst_5 = arith.constant dense<64> : tensor<1x128xi32, #blocked>
+    %cst_6 = arith.constant dense<100> : tensor<1x128xi32, #blocked>
@@ -18,41 +17,34 @@
-    %2 = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<128xi32, #blocked2> -> tensor<128xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<128xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<128x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<128x1xi32, #blocked3> -> tensor<128x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<128x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<128x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_5 : tensor<128x1xi32, #blocked1>
-    %9 = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<128xi32, #blocked2> -> tensor<128xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<128xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x128xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x128xi32, #blocked4> -> tensor<1x128xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x128xi32, #blocked4> -> tensor<1x128xi32, #blocked>
-    %14 = arith.remsi %7, %cst_4 : tensor<128x1xi32, #blocked1>
-    %15 = arith.divsi %7, %cst_4 : tensor<128x1xi32, #blocked1>
-    %16 = arith.cmpi slt, %13, %cst_2 : tensor<1x128xi32, #blocked>
-    %17 = arith.muli %12, %cst_1 : tensor<1x128xi32, #blocked>
-    %18 = tt.broadcast %14 : tensor<128x1xi32, #blocked1> -> tensor<128x128xi32, #blocked1>
-    %19 = ttg.convert_layout %18 : tensor<128x128xi32, #blocked1> -> tensor<128x128xi32, #blocked>
-    %20 = tt.broadcast %17 : tensor<1x128xi32, #blocked> -> tensor<128x128xi32, #blocked>
-    %21 = arith.addi %19, %20 : tensor<128x128xi32, #blocked>
-    %22 = arith.muli %15, %cst_0 : tensor<128x1xi32, #blocked1>
-    %23 = tt.broadcast %22 : tensor<128x1xi32, #blocked1> -> tensor<128x128xi32, #blocked1>
-    %24 = ttg.convert_layout %23 : tensor<128x128xi32, #blocked1> -> tensor<128x128xi32, #blocked>
-    %25 = arith.addi %21, %24 : tensor<128x128xi32, #blocked>
-    %26 = tt.splat %arg0 : !tt.ptr<f32> -> tensor<128x128x!tt.ptr<f32>, #blocked>
-    %27 = tt.addptr %26, %25 : tensor<128x128x!tt.ptr<f32>, #blocked>, tensor<128x128xi32, #blocked>
-    %28 = tt.broadcast %16 : tensor<1x128xi1, #blocked> -> tensor<128x128xi1, #blocked>
-    %29 = tt.broadcast %8 : tensor<128x1xi1, #blocked1> -> tensor<128x128xi1, #blocked1>
-    %30 = ttg.convert_layout %29 : tensor<128x128xi1, #blocked1> -> tensor<128x128xi1, #blocked>
-    %31 = arith.andi %28, %30 : tensor<128x128xi1, #blocked>
-    %32 = tt.load %27, %31, %cst evictionPolicy = evict_first : tensor<128x128x!tt.ptr<f32>, #blocked>
-    %33 = arith.cmpf ogt, %cst_3, %32 : tensor<128x128xf32, #blocked>
-    %34 = arith.cmpf une, %cst_3, %cst_3 : tensor<128x128xf32, #blocked>
-    %35 = arith.ori %33, %34 : tensor<128x128xi1, #blocked>
```

## Inputs

Each compiled pair is run on 20 input draws cycling through 15 fill
modes. The one that differs is **draw 0**: fill mode `nanpay`, fill seed
`96015431`. Every pointer argument is a slot in one deterministically filled
pool, so the mode plus the seed reproduces the bytes exactly -- no tensor files
are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the determinism gate, the measurements, and `fz.repro`) replays
**this** draw, not draw 0.

## Observed

First differing byte in the pool: `6291456` (slot `1`, i.e. pointer
argument index `1`).

## Determinism evidence

Two separate gates, both passed:

1. Pre-screen (PLAN section 4 gate 1), at the kernel's base shape and config:
   the same build run three times on the same input is bit-identical.
2. Re-checked **at this draw's own shape and config**, because round 2 moves
   both: the reference binary gave identical bits on 4/4 runs and the
   candidate binary on 4/4 runs of the same input.

The mismatch itself was re-run 3 times **on the differing draw**, all three
reproduced, then re-run over the full sweep of 16 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 1,
   "dtype": "torch.float32",
   "n": 524288,
   "ndiff": 90,
   "first_index": 0,
   "max_abs_diff": 0.0,
   "max_abs_value": 4.810581207275391,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0x7fab26d3",
   "cand_bits": "0x7fa93131",
   "max_ulp": 0,
   "ndiff_nan_payload": 90,
   "ndiff_value": 0
  }
 ],
 "ndiff_total": 90,
 "n_total": 1048576,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 90,
 "max_abs_diff": 0.0,
 "max_abs_value": 4.810581207275391,
 "max_ulp": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addi`, `arith.andi`, `arith.cmpf`, `arith.cmpi`, `arith.constant`, `arith.divsi`, `arith.muli`, `arith.ori`, `arith.remsi`, `arith.select`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`, `ttg.convert_layout`, `ttg.slice`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: `blocked3`, `blocked4`

**Diff size**: +56 / -64 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(1, 1, 1)` (programs = 1). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: at the *drawn shape and config*, the reference binary
gave identical bits on 4/4 runs of the same
input and the candidate binary on 4/4. The
mismatch reproduced 3/3 on re-run and
`mismatch` over 20 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16lim, int24, signzero, nanpay.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [4, 1], threadsPerWarp = [32, 1], warpsPerCTA = [1, 4], order = [0, 1]}>
@@ -3,3 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [4], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
@@ -8,7 +6,2 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<128x128xf32, #blocked>
-    %cst_0 = arith.constant dense<6400> : tensor<128x1xi32, #blocked1>
-    %cst_1 = arith.constant dense<64> : tensor<1x128xi32, #blocked>
-    %cst_2 = arith.constant dense<100> : tensor<1x128xi32, #blocked>
-    %cst_3 = arith.constant dense<0xFF800000> : tensor<128x128xf32, #blocked>
-    %cst_4 = arith.constant dense<64> : tensor<128x1xi32, #blocked1>
-    %cst_5 = arith.constant dense<128> : tensor<128x1xi32, #blocked1>
+    %cst = arith.constant dense<0xFF800000> : tensor<128x128xf32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<128x128xf32, #blocked>
@@ -15,0 +9,6 @@
+    %cst_1 = arith.constant dense<128> : tensor<128x1xi32, #blocked1>
+    %cst_2 = arith.constant dense<128> : tensor<128x1xi32, #blocked>
+    %cst_3 = arith.constant dense<64> : tensor<128x1xi32, #blocked>
+    %cst_4 = arith.constant dense<6400> : tensor<128x1xi32, #blocked>
+    %cst_5 = arith.constant dense<64> : tensor<1x128xi32, #blocked>
+    %cst_6 = arith.constant dense<100> : tensor<1x128xi32, #blocked>
@@ -18,41 +17,34 @@
-    %2 = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<128xi32, #blocked2> -> tensor<128xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<128xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<128x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<128x1xi32, #blocked3> -> tensor<128x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<128x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<128x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_5 : tensor<128x1xi32, #blocked1>
-    %9 = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<128xi32, #blocked2> -> tensor<128xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<128xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x128xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x128xi32, #blocked4> -> tensor<1x128xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x128xi32, #blocked4> -> tensor<1x128xi32, #blocked>
-    %14 = arith.remsi %7, %cst_4 : tensor<128x1xi32, #blocked1>
-    %15 = arith.divsi %7, %cst_4 : tensor<128x1xi32, #blocked1>
-    %16 = arith.cmpi slt, %13, %cst_2 : tensor<1x128xi32, #blocked>
-    %17 = arith.muli %12, %cst_1 : tensor<1x128xi32, #blocked>
-    %18 = tt.broadcast %14 : tensor<128x1xi32, #blocked1> -> tensor<128x128xi32, #blocked1>
-    %19 = ttg.convert_layout %18 : tensor<128x128xi32, #blocked1> -> tensor<128x128xi32, #blocked>
-    %20 = tt.broadcast %17 : tensor<1x128xi32, #blocked> -> tensor<128x128xi32, #blocked>
-    %21 = arith.addi %19, %20 : tensor<128x128xi32, #blocked>
-    %22 = arith.muli %15, %cst_0 : tensor<128x1xi32, #blocked1>
-    %23 = tt.broadcast %22 : tensor<128x1xi32, #blocked1> -> tensor<128x128xi32, #blocked1>
-    %24 = ttg.convert_layout %23 : tensor<128x128xi32, #blocked1> -> tensor<128x128xi32, #blocked>
-    %25 = arith.addi %21, %24 : tensor<128x128xi32, #blocked>
-    %26 = tt.splat %arg0 : !tt.ptr<f32> -> tensor<128x128x!tt.ptr<f32>, #blocked>
-    %27 = tt.addptr %26, %25 : tensor<128x128x!tt.ptr<f32>, #blocked>, tensor<128x128xi32, #blocked>
-    %28 = tt.broadcast %16 : tensor<1x128xi1, #blocked> -> tensor<128x128xi1, #blocked>
-    %29 = tt.broadcast %8 : tensor<128x1xi1, #blocked1> -> tensor<128x128xi1, #blocked1>
-    %30 = ttg.convert_layout %29 : tensor<128x128xi1, #blocked1> -> tensor<128x128xi1, #blocked>
-    %31 = arith.andi %28, %30 : tensor<128x128xi1, #blocked>
-    %32 = tt.load %27, %31, %cst evictionPolicy = evict_first : tensor<128x128x!tt.ptr<f32>, #blocked>
-    %33 = arith.cmpf ogt, %cst_3, %32 : tensor<128x128xf32, #blocked>
-    %34 = arith.cmpf une, %cst_3, %cst_3 : tensor<128x128xf32, #blocked>
-    %35 = arith.ori %33, %34 : tensor<128x128xi1, #blocked>
```

