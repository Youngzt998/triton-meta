# HIT-0142: triton-licm on ind:triton_red_fused_amin_1_6afabf07 [ttir]
Verdict: CONFIRMED
Arch:    generic
Line:    r2-corpus
Level:   ttir
Culprit pass: `--triton-licm`  (class: bit)
SMT verdict: TODO (reviewing agent)

Summary: compiling `ind:triton_red_fused_amin_1_6afabf07` from the corpus at the shape and compile config
below, the pipeline shown and the same pipeline plus one extra pass produce
cubins whose outputs differ bit for bit on the same input. That is the whole
claim: a bit difference between two compilations. No judgement about whether it
is a compiler bug is made here -- the labels below carry the material a
reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Shape and config draw (round 2)

Round 1 pinned both of these; round 2 draws them per round, identically for
both arms, so they are part of the experiment's setting and never part of the
control.

* config: `w16 cta1 st6 regNone tf32x3`
* shape : `grid=(4, 1, 1)  consts=R0_BLOCK=16`

```json
{
 "num_warps": 16,
 "num_ctas": 1,
 "num_stages": 6,
 "maxnreg": null,
 "default_dot_input_precision": "tf32x3",
 "enable_fp_fusion": false,
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": true
}
```

```json
{
 "align": {},
 "consts": {
  "R0_BLOCK": 16
 },
 "scalars": {},
 "grid": [
  4,
  1,
  1
 ]
}
```

Pointer alignment note: when a base pointer offset is non-zero, the
`tt.divisibility` argument attribute is set to match the address the launcher
really passes (the same rule `JITFunction.run` uses). The compiler is never
told an alignment it does not get, so an unaligned draw is not undefined
behaviour.

## Labels

* pass-class: bit
* level: ttir
* config: w16 cta1 st6 regNone tf32x3
* shape draw: grid=(4, 1, 1)  consts=R0_BLOCK=16
* determinism at this shape/config: ref 4/4, cand 4/4 identical runs of the same binary
* exact-integer control (every input a small whole number, so a plain fp32 sum is exact): goes equal
* all-inputs-equal control (every float input the same number, so a mean / variance / Welford accumulator is exact too): goes equal, but whether it ran meaningfully could NOT be established, so no conclusion is drawn from it: no 4-byte word changed under an all-equal fill
* no reassociation claim: a difference must survive BOTH controls before a reordered fp32 sum can be ruled out.
* reassociation_excluded: false -- no claim is made; a difference must survive BOTH controls before a reordered fp32 sum can be ruled out
* onesish_control: UNINFORMATIVE: equal, but the coverage probe is UNDECIDED (no 4-byte word changed under an all-equal fill), so nothing is concluded from it
* ndiff/total: 33/1048576
* of those, real value differences: 0; NaN-payload-only: 33
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* exact-integer control (every input a small whole number, so a plain fp32 sum is exact): goes equal
* all-inputs-equal control (every float input the same number, so a mean / variance / Welford accumulator is exact too): goes equal, but whether it ran meaningfully could NOT be established, so no conclusion is drawn from it: no 4-byte word changed under an all-equal fill
* no reassociation claim: a difference must survive BOTH controls before a reordered fp32 sum can be ruled out.
* determinism of each arm at this shape/config: reference 4/4 identical
  runs, candidate 4/4 identical runs of the same binary on one input

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-corpus/env.sh
cd /home/youngzt/fuzz/r2-corpus
python -m fz.repro --hit HIT-0142
```

That script rebuilds both sides from the corpus at the recorded shape and
config and reruns the comparison. To do the pass step by hand:

```bash
source /home/youngzt/fuzz/r2-corpus/env.sh
cd /home/youngzt/fuzz/r2-corpus
# reference IR (already saved):  artifacts/HIT-0142/input.ttir
# candidate IR (already saved):  artifacts/HIT-0142/output.ttir
triton-opt artifacts/HIT-0142/input.ttir --triton-licm -o /tmp/HIT-0142.ttir
diff artifacts/HIT-0142/output.ttir /tmp/HIT-0142.ttir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Minimal kernel source

Corpus `inductor`, kernel `ind:triton_red_fused_amin_1_6afabf07`, launch grid `(2, 1, 1)`.
Argument spec (after the shape draw is applied):

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null,
  "shape": null,
  "dtype": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null,
  "shape": null,
  "dtype": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 192,
  "shape": null,
  "dtype": null
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 128,
  "shape": null,
  "dtype": null
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 128,
  "shape": null,
  "dtype": null
 },
 {
  "name": "R0_BLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 128,
  "shape": null,
  "dtype": null
 }
]
```

## Pass pipeline: reference vs candidate

Level: `ttir`. At TTIR level both arms then get the identical
`convert-triton-to-tritongpu` and the identical TTGIR base pipeline
`(none)`, so the only difference between the arms is still exactly the
pass set below.

reference:
```
(none: the raw root TTIR)
```
candidate (same, plus the culprit):
```
--triton-licm
```

## IR

* `artifacts/HIT-0142/root.ttir`   -- the root IR at this shape and config
* `artifacts/HIT-0142/input.ttir`  -- after the reference pipeline
* `artifacts/HIT-0142/output.ttir` -- after the reference pipeline + `--triton-licm`
* `artifacts/HIT-0142/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -1,9 +0,0 @@
-#loc = loc("/home/youngzt/fuzz/r2-corpus/corpus/inductor_kernels.py":2424:1)
-#loc2 = loc(unknown)
-#loc27 = loc("/home/youngzt/fuzz/r2-corpus/corpus/inductor_kernels.py":2448:12)
-#loc30 = loc("in_ptr0"(#loc))
-#loc31 = loc("out_ptr0"(#loc))
-#loc32 = loc("xnumel"(#loc))
-#loc33 = loc("r0_numel"(#loc))
-#loc57 = loc("tmp2"(#loc27))
-#loc63 = loc(callsite(#loc2 at #loc57))
@@ -11,123 +2,65 @@
-  tt.func public @triton_red_fused_amin_1_6afabf07(%in_ptr0: !tt.ptr<f32> {tt.divisibility = 16 : i32} loc("in_ptr0"(#loc)), %out_ptr0: !tt.ptr<f32> {tt.divisibility = 16 : i32} loc("out_ptr0"(#loc)), %xnumel: i32 {tt.divisibility = 16 : i32} loc("xnumel"(#loc)), %r0_numel: i32 {tt.divisibility = 16 : i32} loc("r0_numel"(#loc))) attributes {noinline = false} {
-    %c16_i32 = arith.constant 16 : i32 loc(#loc1)
-    %c0_i32 = arith.constant 0 : i32 loc(#loc1)
-    %cst = arith.constant dense<0.000000e+00> : tensor<128x16xf32> loc(#loc2)
-    %cst_0 = arith.constant dense<12288> : tensor<128x1xi32> loc(#loc2)
-    %cst_1 = arith.constant dense<96> : tensor<1x16xi32> loc(#loc2)
-    %cst_2 = arith.constant dense<128> : tensor<1x16xi32> loc(#loc2)
-    %_tmp2 = arith.constant dense<0x7F800000> : tensor<128x16xf32> loc(#loc34)
-    %cst_3 = arith.constant dense<96> : tensor<128x1xi32> loc(#loc2)
-    %xmask = arith.constant dense<192> : tensor<128x1xi32> loc(#loc35)
-    %c128_i32 = arith.constant 128 : i32 loc(#loc2)
-    %xoffset = tt.get_program_id x : i32 loc(#loc36)
-    %xoffset_4 = arith.muli %xoffset, %c128_i32 : i32 loc(#loc36)
-    %xindex = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32> loc(#loc37)
-    %xindex_5 = tt.expand_dims %xindex {axis = 1 : i32} : tensor<128xi32> -> tensor<128x1xi32> loc(#loc37)
-    %xindex_6 = tt.splat %xoffset_4 : i32 -> tensor<128x1xi32> loc(#loc38)
-    %xindex_7 = arith.addi %xindex_6, %xindex_5 : tensor<128x1xi32> loc(#loc38)
-    %xmask_8 = arith.cmpi slt, %xindex_7, %xmask : tensor<128x1xi32> loc(#loc35)
-    %r0_base = tt.make_range {end = 16 : i32, start = 0 : i32} : tensor<16xi32> loc(#loc39)
-    %r0_base_9 = tt.expand_dims %r0_base {axis = 0 : i32} : tensor<16xi32> -> tensor<1x16xi32> loc(#loc39)
-    %x0 = arith.remsi %xindex_7, %cst_3 : tensor<128x1xi32> loc(#loc40)
-    %x1 = arith.divsi %xindex_7, %cst_3 : tensor<128x1xi32> loc(#loc41)
-    %_tmp2_10 = scf.for %r0_offset = %c0_i32 to %c128_i32 step %c16_i32 iter_args(%_tmp2_12 = %_tmp2) -> (tensor<128x16xf32>)  : i32 {
-      %r0_index = tt.splat %r0_offset : i32 -> tensor<1x16xi32> loc(#loc43)
-      %r0_index_13 = arith.addi %r0_index, %r0_base_9 : tensor<1x16xi32> loc(#loc43)
-      %r0_mask = arith.cmpi slt, %r0_index_13, %cst_2 : tensor<1x16xi32> loc(#loc44)
-      %tmp0 = arith.muli %r0_index_13, %cst_1 : tensor<1x16xi32> loc(#loc45)
-      %tmp0_14 = tt.broadcast %x0 : tensor<128x1xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_15 = tt.broadcast %tmp0 : tensor<1x16xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_16 = arith.addi %tmp0_14, %tmp0_15 : tensor<128x16xi32> loc(#loc46)
-      %tmp0_17 = arith.muli %x1, %cst_0 : tensor<128x1xi32> loc(#loc47)
-      %tmp0_18 = tt.broadcast %tmp0_17 : tensor<128x1xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_19 = arith.addi %tmp0_16, %tmp0_18 : tensor<128x16xi32> loc(#loc46)
-      %tmp0_20 = tt.splat %in_ptr0 : !tt.ptr<f32> -> tensor<128x16x!tt.ptr<f32>> loc(#loc48)
-      %tmp0_21 = tt.addptr %tmp0_20, %tmp0_19 : tensor<128x16x!tt.ptr<f32>>, tensor<128x16xi32> loc(#loc48)
-      %tmp0_22 = tt.broadcast %r0_mask : tensor<1x16xi1> -> tensor<128x16xi1> loc(#loc49)
-      %tmp0_23 = tt.broadcast %xmask_8 : tensor<128x1xi1> -> tensor<128x16xi1> loc(#loc49)
-      %tmp0_24 = arith.andi %tmp0_22, %tmp0_23 : tensor<128x16xi1> loc(#loc49)
-      %tmp0_25 = tt.load %tmp0_21, %tmp0_24, %cst evictionPolicy = evict_first : tensor<128x16x!tt.ptr<f32>> loc(#loc50)
-      %mask = arith.cmpf olt, %_tmp2_12, %tmp0_25 : tensor<128x16xf32> loc(#loc58)
-      %mask_26 = arith.cmpf une, %_tmp2_12, %_tmp2_12 : tensor<128x16xf32> loc(#loc59)
-      %mask_27 = arith.ori %mask, %mask_26 : tensor<128x16xi1> loc(#loc60)
-      %tmp3 = arith.select %mask_27, %_tmp2_12, %tmp0_25 : tensor<128x16xi1>, tensor<128x16xf32> loc(#loc61)
-      %_tmp2_28 = tt.broadcast %r0_mask : tensor<1x16xi1> -> tensor<128x16xi1> loc(#loc55)
-      %_tmp2_29 = tt.broadcast %xmask_8 : tensor<128x1xi1> -> tensor<128x16xi1> loc(#loc55)
-      %_tmp2_30 = arith.andi %_tmp2_28, %_tmp2_29 : tensor<128x16xi1> loc(#loc55)
-      %_tmp2_31 = arith.select %_tmp2_30, %tmp3, %_tmp2_12 : tensor<128x16xi1>, tensor<128x16xf32> loc(#loc56)
-      scf.yield %_tmp2_31 : tensor<128x16xf32> loc(#loc1)
-    } loc(#loc42)
```

## Inputs

Each compiled pair is run on 20 input draws cycling through 15 fill
modes. The one that differs is **draw 3**: fill mode `extreme2`, fill seed
`79902713`. Every pointer argument is a slot in one deterministically filled
pool, so the mode plus the seed reproduces the bytes exactly -- no tensor files
are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the determinism gate, the measurements, and `fz.repro`) replays
**this** draw, not draw 0.

## Observed

First differing byte in the pool: `6291504` (slot `1`, i.e. pointer
argument index `1`).

## Determinism evidence

Two separate gates, both passed:

1. Pre-screen (PLAN section 4 gate 1), at the kernel's base shape and config:
   the same build run three times on the same input is bit-identical.
2. Re-checked **at this draw's own shape and config**, because round 2 moves
   both: the reference binary gave identical bits on 4/4 runs and the
   candidate binary on 4/4 runs of the same input.

The mismatch itself was re-run 3 times **on the differing draw**, all three
reproduced, then re-run over the full sweep of 16 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 1,
   "dtype": "torch.float32",
   "n": 524288,
   "ndiff": 33,
   "first_index": 12,
   "max_abs_diff": 0.0,
   "max_abs_value": 9.999999680285692e+37,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0x7fc00000",
   "cand_bits": "0x7fffffff",
   "max_ulp": 0,
   "ndiff_nan_payload": 33,
   "ndiff_value": 0
  }
 ],
 "ndiff_total": 33,
 "n_total": 1048576,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 33,
 "max_abs_diff": 0.0,
 "max_abs_value": 9.999999680285692e+37,
 "max_ulp": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `bit` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addi`, `arith.andi`, `arith.cmpf`, `arith.cmpi`, `arith.constant`, `arith.divsi`, `arith.muli`, `arith.ori`, `arith.remsi`, `arith.select`, `scf.for`, `scf.yield`, `tt.addptr`, `tt.broadcast`, `tt.divisibility`, `tt.expand_dims`, `tt.func`, `tt.get_program_id`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.return`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)

**Diff size**: +65 / -132 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `True`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(2, 1, 1)` (programs = 2). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: at the *drawn shape and config*, the reference binary
gave identical bits on 4/4 runs of the same
input and the candidate binary on 4/4. The
mismatch reproduced 3/3 on re-run and
`mismatch` over 20 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16lim, int24, signzero, nanpay.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -1,9 +0,0 @@
-#loc = loc("/home/youngzt/fuzz/r2-corpus/corpus/inductor_kernels.py":2424:1)
-#loc2 = loc(unknown)
-#loc27 = loc("/home/youngzt/fuzz/r2-corpus/corpus/inductor_kernels.py":2448:12)
-#loc30 = loc("in_ptr0"(#loc))
-#loc31 = loc("out_ptr0"(#loc))
-#loc32 = loc("xnumel"(#loc))
-#loc33 = loc("r0_numel"(#loc))
-#loc57 = loc("tmp2"(#loc27))
-#loc63 = loc(callsite(#loc2 at #loc57))
@@ -11,123 +2,65 @@
-  tt.func public @triton_red_fused_amin_1_6afabf07(%in_ptr0: !tt.ptr<f32> {tt.divisibility = 16 : i32} loc("in_ptr0"(#loc)), %out_ptr0: !tt.ptr<f32> {tt.divisibility = 16 : i32} loc("out_ptr0"(#loc)), %xnumel: i32 {tt.divisibility = 16 : i32} loc("xnumel"(#loc)), %r0_numel: i32 {tt.divisibility = 16 : i32} loc("r0_numel"(#loc))) attributes {noinline = false} {
-    %c16_i32 = arith.constant 16 : i32 loc(#loc1)
-    %c0_i32 = arith.constant 0 : i32 loc(#loc1)
-    %cst = arith.constant dense<0.000000e+00> : tensor<128x16xf32> loc(#loc2)
-    %cst_0 = arith.constant dense<12288> : tensor<128x1xi32> loc(#loc2)
-    %cst_1 = arith.constant dense<96> : tensor<1x16xi32> loc(#loc2)
-    %cst_2 = arith.constant dense<128> : tensor<1x16xi32> loc(#loc2)
-    %_tmp2 = arith.constant dense<0x7F800000> : tensor<128x16xf32> loc(#loc34)
-    %cst_3 = arith.constant dense<96> : tensor<128x1xi32> loc(#loc2)
-    %xmask = arith.constant dense<192> : tensor<128x1xi32> loc(#loc35)
-    %c128_i32 = arith.constant 128 : i32 loc(#loc2)
-    %xoffset = tt.get_program_id x : i32 loc(#loc36)
-    %xoffset_4 = arith.muli %xoffset, %c128_i32 : i32 loc(#loc36)
-    %xindex = tt.make_range {end = 128 : i32, start = 0 : i32} : tensor<128xi32> loc(#loc37)
-    %xindex_5 = tt.expand_dims %xindex {axis = 1 : i32} : tensor<128xi32> -> tensor<128x1xi32> loc(#loc37)
-    %xindex_6 = tt.splat %xoffset_4 : i32 -> tensor<128x1xi32> loc(#loc38)
-    %xindex_7 = arith.addi %xindex_6, %xindex_5 : tensor<128x1xi32> loc(#loc38)
-    %xmask_8 = arith.cmpi slt, %xindex_7, %xmask : tensor<128x1xi32> loc(#loc35)
-    %r0_base = tt.make_range {end = 16 : i32, start = 0 : i32} : tensor<16xi32> loc(#loc39)
-    %r0_base_9 = tt.expand_dims %r0_base {axis = 0 : i32} : tensor<16xi32> -> tensor<1x16xi32> loc(#loc39)
-    %x0 = arith.remsi %xindex_7, %cst_3 : tensor<128x1xi32> loc(#loc40)
-    %x1 = arith.divsi %xindex_7, %cst_3 : tensor<128x1xi32> loc(#loc41)
-    %_tmp2_10 = scf.for %r0_offset = %c0_i32 to %c128_i32 step %c16_i32 iter_args(%_tmp2_12 = %_tmp2) -> (tensor<128x16xf32>)  : i32 {
-      %r0_index = tt.splat %r0_offset : i32 -> tensor<1x16xi32> loc(#loc43)
-      %r0_index_13 = arith.addi %r0_index, %r0_base_9 : tensor<1x16xi32> loc(#loc43)
-      %r0_mask = arith.cmpi slt, %r0_index_13, %cst_2 : tensor<1x16xi32> loc(#loc44)
-      %tmp0 = arith.muli %r0_index_13, %cst_1 : tensor<1x16xi32> loc(#loc45)
-      %tmp0_14 = tt.broadcast %x0 : tensor<128x1xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_15 = tt.broadcast %tmp0 : tensor<1x16xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_16 = arith.addi %tmp0_14, %tmp0_15 : tensor<128x16xi32> loc(#loc46)
-      %tmp0_17 = arith.muli %x1, %cst_0 : tensor<128x1xi32> loc(#loc47)
-      %tmp0_18 = tt.broadcast %tmp0_17 : tensor<128x1xi32> -> tensor<128x16xi32> loc(#loc46)
-      %tmp0_19 = arith.addi %tmp0_16, %tmp0_18 : tensor<128x16xi32> loc(#loc46)
-      %tmp0_20 = tt.splat %in_ptr0 : !tt.ptr<f32> -> tensor<128x16x!tt.ptr<f32>> loc(#loc48)
-      %tmp0_21 = tt.addptr %tmp0_20, %tmp0_19 : tensor<128x16x!tt.ptr<f32>>, tensor<128x16xi32> loc(#loc48)
-      %tmp0_22 = tt.broadcast %r0_mask : tensor<1x16xi1> -> tensor<128x16xi1> loc(#loc49)
-      %tmp0_23 = tt.broadcast %xmask_8 : tensor<128x1xi1> -> tensor<128x16xi1> loc(#loc49)
-      %tmp0_24 = arith.andi %tmp0_22, %tmp0_23 : tensor<128x16xi1> loc(#loc49)
-      %tmp0_25 = tt.load %tmp0_21, %tmp0_24, %cst evictionPolicy = evict_first : tensor<128x16x!tt.ptr<f32>> loc(#loc50)
-      %mask = arith.cmpf olt, %_tmp2_12, %tmp0_25 : tensor<128x16xf32> loc(#loc58)
-      %mask_26 = arith.cmpf une, %_tmp2_12, %_tmp2_12 : tensor<128x16xf32> loc(#loc59)
-      %mask_27 = arith.ori %mask, %mask_26 : tensor<128x16xi1> loc(#loc60)
-      %tmp3 = arith.select %mask_27, %_tmp2_12, %tmp0_25 : tensor<128x16xi1>, tensor<128x16xf32> loc(#loc61)
-      %_tmp2_28 = tt.broadcast %r0_mask : tensor<1x16xi1> -> tensor<128x16xi1> loc(#loc55)
-      %_tmp2_29 = tt.broadcast %xmask_8 : tensor<128x1xi1> -> tensor<128x16xi1> loc(#loc55)
-      %_tmp2_30 = arith.andi %_tmp2_28, %_tmp2_29 : tensor<128x16xi1> loc(#loc55)
-      %_tmp2_31 = arith.select %_tmp2_30, %tmp3, %_tmp2_12 : tensor<128x16xi1>, tensor<128x16xf32> loc(#loc56)
-      scf.yield %_tmp2_31 : tensor<128x16xf32> loc(#loc1)
-    } loc(#loc42)
```

