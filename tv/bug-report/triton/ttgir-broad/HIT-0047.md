# HIT-0047: triton-loop-aware-cse on bch:_segment_reduce_backward_kernel_60974cef
Verdict: CONFIRMED
Arch:    generic
Line:    ttgir-broad
Culprit pass: `--triton-loop-aware-cse`  (class: bit)
SMT verdict: TODO (reviewing agent)

Summary: compiling `bch:_segment_reduce_backward_kernel_60974cef` from the corpus, the pipeline below and the same
pipeline plus one extra pass produce cubins whose outputs differ bit for bit on
the same input. That is the whole claim: a bit difference between two
compilations. No judgement about whether it is a compiler bug is made here --
the labels below carry the material a reviewer needs. The kernel is pointwise (no tt.dot, no tt.reduce).

## Labels

* pass-class: bit
* of those, real value differences: None; NaN-payload-only: None
* max_abs_diff: None
* max_ulp: None

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/ttgir-broad/env.sh
cd /home/youngzt/fuzz/ttgir-broad
python -m fz.repro --hit HIT-0047
```

That script rebuilds both sides from the corpus and reruns the comparison. To do
it by hand:

```bash
source /home/youngzt/fuzz/ttgir-broad/env.sh
cd /home/youngzt/fuzz/ttgir-broad
# reference IR (already saved):  artifacts/HIT-0047/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0047/output.ttgir
triton-opt artifacts/HIT-0047/input.ttgir --triton-loop-aware-cse -o /tmp/HIT-0047.ttgir
diff artifacts/HIT-0047/output.ttgir /tmp/HIT-0047.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Minimal kernel source

Corpus `bench`, kernel `bch:_segment_reduce_backward_kernel_60974cef`, launch grid `(3, 1, 1)`.
Argument spec:

```json
[
 {
  "name": "grad",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   3
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "output",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   3
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "data",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   5
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "offsets",
  "kind": "ptr",
  "ty": "*i64",
  "value": null,
  "shape": [
   4
  ],
  "dtype": "torch.int64"
 },
 {
  "name": "grad_input",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   5
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "segment_count",
  "kind": "int",
  "ty": "i32",
  "value": 3,
  "shape": null,
  "dtype": null
 },
 {
  "name": "inner_size",
  "kind": "int",
  "ty": "i32",
  "value": 1,
  "shape": null,
  "dtype": null
 },
 {
  "name": "data_size_axis",
  "kind": "int",
  "ty": "i32",
  "value": 5,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_SUM",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": true,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_MEAN",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": false,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_MAX",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": false,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_MIN",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": false,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_PROD",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": false,
  "shape": null,
  "dtype": null
 },
 {
  "name": "INITIAL_PROD_VALUE",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 1.0,
  "shape": null,
  "dtype": null
 },
 {
  "name": "BLOCK_SIZE",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 1024,
  "shape": null,
  "dtype": null
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
--tritongpu-optimize-dot-operands \
      --triton-combine
```
candidate (same, plus the culprit):
```
--tritongpu-optimize-dot-operands \
      --triton-combine \
      --triton-loop-aware-cse
```

## IR

* `artifacts/HIT-0047/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0047/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0047/output.ttgir` -- after the reference pipeline + `--triton-loop-aware-cse`
* `artifacts/HIT-0047/facts.json`   -- mechanical facts about the diff

First 40 lines of the diff:

```mlir
@@ -11,11 +11,11 @@
-    %4 = arith.extsi %arg6 : i32 to i64
-    %5 = arith.divsi %1, %4 : i64
-    %6 = arith.extsi %arg5 : i32 to i64
-    %7 = arith.remsi %5, %6 : i64
-    %8 = arith.extsi %arg5 : i32 to i64
-    %9 = arith.divsi %5, %8 : i64
-    %10 = arith.addi %arg5, %c1_i32 : i32
-    %11 = arith.extsi %10 : i32 to i64
-    %12 = arith.muli %9, %11 : i64
-    %13 = arith.addi %12, %7 : i64
-    %14 = tt.addptr %arg3, %13 : !tt.ptr<i64>, i64
+    %4 = arith.divsi %1, %2 : i64
+    %5 = arith.extsi %arg5 : i32 to i64
+    %6 = arith.remsi %4, %5 : i64
+    %7 = arith.divsi %4, %5 : i64
+    %8 = arith.addi %arg5, %c1_i32 : i32
+    %9 = arith.extsi %8 : i32 to i64
+    %10 = arith.muli %7, %9 : i64
+    %11 = arith.addi %10, %6 : i64
+    %12 = tt.addptr %arg3, %11 : !tt.ptr<i64>, i64
+    %13 = tt.load %12 : !tt.ptr<i64>
+    %14 = tt.addptr %12, %c1_i32 : !tt.ptr<i64>, i32
@@ -23,12 +23,9 @@
-    %16 = tt.addptr %arg3, %13 : !tt.ptr<i64>, i64
-    %17 = tt.addptr %16, %c1_i32 : !tt.ptr<i64>, i32
-    %18 = tt.load %17 : !tt.ptr<i64>
-    %19 = arith.subi %18, %15 : i64
-    %20 = arith.cmpi sgt, %19, %c0_i64 : i64
-    scf.if %20 {
-      %21 = tt.addptr %arg0, %1 : !tt.ptr<f16>, i64
-      %22 = tt.load %21 : !tt.ptr<f16>
-      %23 = arith.extf %22 : f16 to f32
-      %24 = scf.while (%arg8 = %15) : (i64) -> i64 {
-        %25 = arith.cmpi slt, %arg8, %18 : i64
-        scf.condition(%25) %arg8 : i64
+    %16 = arith.subi %15, %13 : i64
+    %17 = arith.cmpi sgt, %16, %c0_i64 : i64
+    scf.if %17 {
+      %18 = tt.addptr %arg0, %1 : !tt.ptr<f16>, i64
+      %19 = tt.load %18 : !tt.ptr<f16>
+      %20 = arith.extf %19 : f16 to f32
+      %21 = scf.while (%arg8 = %13) : (i64) -> i64 {
+        %22 = arith.cmpi slt, %arg8, %15 : i64
+        scf.condition(%22) %arg8 : i64
@@ -37,24 +34,22 @@
-        %25 = tt.make_range {end = 1024 : i32, start = 0 : i32} : tensor<1024xi32, #blocked>
-        %26 = arith.extsi %25 : tensor<1024xi32, #blocked> to tensor<1024xi64, #blocked>
-        %27 = tt.splat %arg8 : i64 -> tensor<1024xi64, #blocked>
-        %28 = arith.addi %27, %26 : tensor<1024xi64, #blocked>
-        %29 = tt.splat %18 : i64 -> tensor<1024xi64, #blocked>
-        %30 = arith.cmpi slt, %28, %29 : tensor<1024xi64, #blocked>
-        %31 = arith.extsi %arg7 : i32 to i64
-        %32 = arith.muli %9, %31 : i64
-        %33 = arith.extsi %arg6 : i32 to i64
-        %34 = arith.muli %32, %33 : i64
-        %35 = arith.extsi %arg6 : i32 to i64
-        %36 = tt.splat %35 : i64 -> tensor<1024xi64, #blocked>
-        %37 = arith.muli %28, %36 : tensor<1024xi64, #blocked>
-        %38 = tt.splat %34 : i64 -> tensor<1024xi64, #blocked>
```

## Inputs

Generated by `fz.argsgen`: fill mode `intval`, seed `8254631`. Every pointer
argument is a slot in one deterministically filled pool, so the seed plus the
mode reproduces the bytes exactly. No tensor files are needed.

## Observed

First differing byte in the pool: `12582927` (slot `4`, i.e. pointer
argument index `4`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1). The mismatch was re-run 3 times, all three reproduced, then re-run over
16 further inputs: verdict `"mismatch"`.

## Difference measurements

```json
{}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `bit` by `fz/passes.py`. Reduction or dot
present in the reference IR: `False`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addi`, `arith.cmpi`, `arith.divsi`, `arith.extf`, `arith.extsi`, `arith.muli`, `arith.remsi`, `arith.subi`, `arith.truncf`, `scf.condition`, `scf.if`, `scf.while`, `scf.yield`, `tt.addptr`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.splat`, `tt.store`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)

**Diff size**: +42 / -47 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `False`, loop = `True`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(3, 1, 1)` (programs = 3). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: same build run 3x on one input is bit-identical
(pre-screen); the mismatch reproduced 3/3 on re-run and
`mismatch` over 16 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 40 diff lines**

```mlir
@@ -11,11 +11,11 @@
-    %4 = arith.extsi %arg6 : i32 to i64
-    %5 = arith.divsi %1, %4 : i64
-    %6 = arith.extsi %arg5 : i32 to i64
-    %7 = arith.remsi %5, %6 : i64
-    %8 = arith.extsi %arg5 : i32 to i64
-    %9 = arith.divsi %5, %8 : i64
-    %10 = arith.addi %arg5, %c1_i32 : i32
-    %11 = arith.extsi %10 : i32 to i64
-    %12 = arith.muli %9, %11 : i64
-    %13 = arith.addi %12, %7 : i64
-    %14 = tt.addptr %arg3, %13 : !tt.ptr<i64>, i64
+    %4 = arith.divsi %1, %2 : i64
+    %5 = arith.extsi %arg5 : i32 to i64
+    %6 = arith.remsi %4, %5 : i64
+    %7 = arith.divsi %4, %5 : i64
+    %8 = arith.addi %arg5, %c1_i32 : i32
+    %9 = arith.extsi %8 : i32 to i64
+    %10 = arith.muli %7, %9 : i64
+    %11 = arith.addi %10, %6 : i64
+    %12 = tt.addptr %arg3, %11 : !tt.ptr<i64>, i64
+    %13 = tt.load %12 : !tt.ptr<i64>
+    %14 = tt.addptr %12, %c1_i32 : !tt.ptr<i64>, i32
@@ -23,12 +23,9 @@
-    %16 = tt.addptr %arg3, %13 : !tt.ptr<i64>, i64
-    %17 = tt.addptr %16, %c1_i32 : !tt.ptr<i64>, i32
-    %18 = tt.load %17 : !tt.ptr<i64>
-    %19 = arith.subi %18, %15 : i64
-    %20 = arith.cmpi sgt, %19, %c0_i64 : i64
-    scf.if %20 {
-      %21 = tt.addptr %arg0, %1 : !tt.ptr<f16>, i64
-      %22 = tt.load %21 : !tt.ptr<f16>
-      %23 = arith.extf %22 : f16 to f32
-      %24 = scf.while (%arg8 = %15) : (i64) -> i64 {
-        %25 = arith.cmpi slt, %arg8, %18 : i64
-        scf.condition(%25) %arg8 : i64
+    %16 = arith.subi %15, %13 : i64
+    %17 = arith.cmpi sgt, %16, %c0_i64 : i64
+    scf.if %17 {
+      %18 = tt.addptr %arg0, %1 : !tt.ptr<f16>, i64
+      %19 = tt.load %18 : !tt.ptr<f16>
+      %20 = arith.extf %19 : f16 to f32
+      %21 = scf.while (%arg8 = %13) : (i64) -> i64 {
+        %22 = arith.cmpi slt, %arg8, %15 : i64
+        scf.condition(%22) %arg8 : i64
@@ -37,24 +34,22 @@
-        %25 = tt.make_range {end = 1024 : i32, start = 0 : i32} : tensor<1024xi32, #blocked>
-        %26 = arith.extsi %25 : tensor<1024xi32, #blocked> to tensor<1024xi64, #blocked>
-        %27 = tt.splat %arg8 : i64 -> tensor<1024xi64, #blocked>
-        %28 = arith.addi %27, %26 : tensor<1024xi64, #blocked>
-        %29 = tt.splat %18 : i64 -> tensor<1024xi64, #blocked>
-        %30 = arith.cmpi slt, %28, %29 : tensor<1024xi64, #blocked>
-        %31 = arith.extsi %arg7 : i32 to i64
-        %32 = arith.muli %9, %31 : i64
-        %33 = arith.extsi %arg6 : i32 to i64
-        %34 = arith.muli %32, %33 : i64
-        %35 = arith.extsi %arg6 : i32 to i64
-        %36 = tt.splat %35 : i64 -> tensor<1024xi64, #blocked>
-        %37 = arith.muli %28, %36 : tensor<1024xi64, #blocked>
-        %38 = tt.splat %34 : i64 -> tensor<1024xi64, #blocked>
```

