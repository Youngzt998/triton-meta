# HIT-0016: tritongpu-allocate-warp-groups on bch:_index_put_jit_function_3dacf625
Verdict: CONFIRMED
Arch:    generic
Line:    ttgir-broad
Culprit pass: `--tritongpu-allocate-warp-groups`  (class: bit)
SMT verdict: TODO (reviewing agent)

Summary: compiling `bch:_index_put_jit_function_3dacf625` from the corpus, the pipeline below and the same
pipeline plus one extra pass produce cubins whose outputs differ bit for bit on
the same input. That is the whole claim: a bit difference between two
compilations. No judgement about whether it is a compiler bug is made here --
the labels below carry the material a reviewer needs. The kernel is pointwise (no tt.dot, no tt.reduce).

## Labels

* pass-class: bit
* ndiff/total: 58/9437184
* of those, real value differences: 58; NaN-payload-only: 0
* max_abs_diff: 8.0
* max_ulp: 34816

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/ttgir-broad/env.sh
cd /home/youngzt/fuzz/ttgir-broad
python -m fz.repro --hit HIT-0016
```

That script rebuilds both sides from the corpus and reruns the comparison. To do
it by hand:

```bash
source /home/youngzt/fuzz/ttgir-broad/env.sh
cd /home/youngzt/fuzz/ttgir-broad
# reference IR (already saved):  artifacts/HIT-0016/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0016/output.ttgir
triton-opt artifacts/HIT-0016/input.ttgir --tritongpu-allocate-warp-groups -o /tmp/HIT-0016.ttgir
diff artifacts/HIT-0016/output.ttgir /tmp/HIT-0016.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Minimal kernel source

Corpus `bench`, kernel `bch:_index_put_jit_function_3dacf625`, launch grid `(2, 2, 1)`.
Argument spec:

```json
[
 {
  "name": "input_ptr",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   64,
   64,
   64
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "indices0_ptr",
  "kind": "ptr",
  "ty": "*i64",
  "value": null,
  "shape": [
   4
  ],
  "dtype": "torch.int64"
 },
 {
  "name": "values_ptr",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null,
  "shape": [
   4,
   64,
   64
  ],
  "dtype": "torch.float16"
 },
 {
  "name": "input_shape0",
  "kind": "int",
  "ty": "i32",
  "value": 64,
  "shape": null,
  "dtype": null
 },
 {
  "name": "input_shape1",
  "kind": "int",
  "ty": "i32",
  "value": 64,
  "shape": null,
  "dtype": null
 },
 {
  "name": "input_shape2",
  "kind": "int",
  "ty": "i32",
  "value": 64,
  "shape": null,
  "dtype": null
 },
 {
  "name": "indices0_shape0",
  "kind": "int",
  "ty": "i32",
  "value": 4,
  "shape": null,
  "dtype": null
 },
 {
  "name": "input_stride0",
  "kind": "int",
  "ty": "i32",
  "value": 64,
  "shape": null,
  "dtype": null
 },
 {
  "name": "input_stride1",
  "kind": "int",
  "ty": "i32",
  "value": 4096,
  "shape": null,
  "dtype": null
 },
 {
  "name": "input_stride2",
  "kind": "int",
  "ty": "i32",
  "value": 1,
  "shape": null,
  "dtype": null
 },
 {
  "name": "indices0_stride0",
  "kind": "int",
  "ty": "i32",
  "value": 1,
  "shape": null,
  "dtype": null
 },
 {
  "name": "values_stride0",
  "kind": "int",
  "ty": "i32",
  "value": 64,
  "shape": null,
  "dtype": null
 },
 {
  "name": "values_stride1",
  "kind": "int",
  "ty": "i32",
  "value": 256,
  "shape": null,
  "dtype": null
 },
 {
  "name": "values_stride2",
  "kind": "int",
  "ty": "i32",
  "value": 1,
  "shape": null,
  "dtype": null
 },
 {
  "name": "M",
  "kind": "int",
  "ty": "i32",
  "value": 4,
  "shape": null,
  "dtype": null
 },
 {
  "name": "N",
  "kind": "int",
  "ty": "i32",
  "value": 4096,
  "shape": null,
  "dtype": null
 },
 {
  "name": "IS_ACCUMULATE",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": false,
  "shape": null,
  "dtype": null
 },
 {
  "name": "BLOCK_SIZE0",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 2,
  "shape": null,
  "dtype": null
 },
 {
  "name": "BLOCK_SIZE1",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 2048,
  "shape": null,
  "dtype": null
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--tritongpu-allocate-warp-groups
```

## IR

* `artifacts/HIT-0016/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0016/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0016/output.ttgir` -- after the reference pipeline + `--tritongpu-allocate-warp-groups`
* `artifacts/HIT-0016/facts.json`   -- mechanical facts about the diff

First 40 lines of the diff:

```mlir
@@ -6 +6 @@
-module attributes {"ttg.num-ctas" = 1 : i32, "ttg.num-warps" = 4 : i32, ttg.target = "cuda:90", "ttg.threads-per-warp" = 32 : i32} {
+module attributes {"ttg.num-ctas" = 1 : i32, "ttg.num-warps" = 4 : i32, ttg.target = "cuda:90", "ttg.threads-per-warp" = 32 : i32, "ttg.total-num-warps" = 4 : i32} {
```

## Inputs

Generated by `fz.argsgen`: fill mode `smallint`, seed `660895`. Every pointer
argument is a slot in one deterministically filled pool, so the seed plus the
mode reproduces the bytes exactly. No tensor files are needed.

## Observed

First differing byte in the pool: `16777345` (slot `0`, i.e. pointer
argument index `0`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1). The mismatch was re-run 3 times, all three reproduced, then re-run over
16 further inputs: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 0,
   "dtype": "torch.float16",
   "n": 4194304,
   "ndiff": 58,
   "first_index": 237632,
   "max_abs_diff": 8.0,
   "max_abs_value": 4.0,
   "ref_value": -4.0,
   "cand_value": 0.0,
   "ref_bits": "0xc400",
   "cand_bits": "0x0",
   "max_ulp": 34816,
   "ndiff_nan_payload": 0,
   "ndiff_value": 58,
   "first_value_index": 237632
  }
 ],
 "ndiff_total": 58,
 "n_total": 9437184,
 "ndiff_value_total": 58,
 "ndiff_nan_payload_total": 0,
 "max_abs_diff": 8.0,
 "max_abs_value": 4.0,
 "max_ulp": 34816
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `bit` by `fz/passes.py`. Reduction or dot
present in the reference IR: `False`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: `ttg.total`
* only in the input IR: (none)
* all ops touched by the diff: `ttg.num`, `ttg.target`, `ttg.threads`, `ttg.total`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)

**Diff size**: +1 / -1 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `False`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(2, 2, 1)` (programs = 4). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: same build run 3x on one input is bit-identical
(pre-screen); the mismatch reproduced 3/3 on re-run and
`mismatch` over 16 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 40 diff lines**

```mlir
@@ -6 +6 @@
-module attributes {"ttg.num-ctas" = 1 : i32, "ttg.num-warps" = 4 : i32, ttg.target = "cuda:90", "ttg.threads-per-warp" = 32 : i32} {
+module attributes {"ttg.num-ctas" = 1 : i32, "ttg.num-warps" = 4 : i32, ttg.target = "cuda:90", "ttg.threads-per-warp" = 32 : i32, "ttg.total-num-warps" = 4 : i32} {
```

