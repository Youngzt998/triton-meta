# HIT-0146: tritongpu-remove-layout-conversions on gen:triton_per_fused__to_copy_amax_eq_mean_sub_view_0_2cef5045
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--tritongpu-remove-layout-conversions`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=tf32, enable_fp_fusion=False, launch_cooperative_grid=True, launch_pdl=False, maxnreg=None, num_ctas=1, num_stages=7, num_warps=8, sanitize_overflow=False
* base-pointer alignment: aligned (element offsets [0, 0, 0])
* shape draw: numels={'xnumel': 4, 'r0_numel': 1} blocks={'XBLOCK': 2} grid=[2, 1, 1]
* layout-reorders-reduction
* ndiff/total: 3/229376
* of those, real value differences: 3; NaN-payload-only: 0
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0146
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0146/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0146/output.ttgir
triton-opt artifacts/HIT-0146/input.ttgir --tritongpu-remove-layout-conversions -o /tmp/HIT-0146.ttgir
diff artifacts/HIT-0146/output.ttgir /tmp/HIT-0146.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **2918**  (`python -m fz.progen 2918` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_per_fused__to_copy_amax_eq_mean_sub_view_0`, heuristic `persistent_reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": true,
 "max_autotune": false,
 "persistent_reductions": null,
 "cooperative_reductions": null,
 "multi_kernel": null,
 "combo_kernels": null,
 "prefer_nd_tiling": true,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": null,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0146/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 23107642
import torch
import torch.nn.functional as F

SEED = 23107642
KNOBS = {'dynamic': False, 'backward': True, 'max_autotune': False, 'persistent_reductions': None, 'cooperative_reductions': None, 'multi_kernel': None, 'combo_kernels': None, 'prefer_nd_tiling': True, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': None, 'benchmark_kernel': False}
BACKWARD = True


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(23107642)
    a0 = torch.randn((11, 63, 1024), device=device, dtype=torch.float32, generator=g).to(torch.float16)
    a0.requires_grad_(True)
    return [a0]


def prog(a0):
    v1 = torch.std(a0.float(), dim=0, keepdim=True)
    v2 = torch.relu(v1)
    v3 = v2.masked_fill(v2 < -0.084, float('-inf'))
    v4 = F.elu(a0)
    v5 = torch.reciprocal(v4 + 2.0)
    v6 = torch.sigmoid(v5)
    v7 = v6 * v5
    v8 = (a0.float() - a0.float().mean(-1, keepdim=True))
    v9 = torch.mean(v6.float(), dim=1, keepdim=False)
    v10 = v7.reshape((48, 14784))
    v11 = (v10 != v10)
    v12 = torch.where(v11, v10, v10)
    v13 = torch.amax(v8, dim=2, keepdim=False)
    return v13.float().sum()

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0146/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 2918
# source file  : cyqex3pj5qtu5vp7bb3d2hsw2vz4svuil5rkbbabvrkc4k2gcjbi.py
# inductor name: triton_per_fused__to_copy_amax_eq_mean_sub_view_0
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_per_fused__to_copy_amax_eq_mean_sub_view_0_2cef5045(in_ptr0, out_ptr1, out_ptr2, xnumel, r0_numel, XBLOCK : tl.constexpr):
    xnumel = 4
    r0_numel = 1
    R0_BLOCK: tl.constexpr = 1024
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = xindex < xnumel
    r0_index = tl.arange(0, R0_BLOCK)[None, :]
    r0_offset = 0
    r0_mask = tl.full([R0_BLOCK], True, tl.int1)[None, :]
    roffset = r0_offset
    rindex = r0_index
    r0_1 = r0_index
    x0 = xindex
    tmp0 = tl.load(in_ptr0 + (r0_1 + 1024*x0), xmask, eviction_policy='evict_first', other=0.0).to(tl.float32)
    tmp1 = tmp0.to(tl.float32)
    tmp2 = tl.broadcast_to(tmp1, [XBLOCK, R0_BLOCK])
    tmp4 = tl.where(xmask, tmp2, 0)
    tmp5 = tl.sum(tmp4, 1)[:, None].to(tl.float32)
    tmp6 = tl.full([1, 1], 1024.0, tl.float32)
    tmp7 = (tmp5 / tmp6)
    tmp8 = tmp1 - tmp7
    tmp9 = tl.broadcast_to(tmp8, [XBLOCK, R0_BLOCK])
    tmp11 = tl.where(xmask, tmp9, float("-inf"))
    tmp12 = triton_helpers.max2(tmp11, 1)[:, None].to(tl.float32)
    tmp13 = tmp12 == tmp8
    tl.store(out_ptr2 + (r0_1 + 1024*x0), tmp13, xmask)
    tl.store(out_ptr1 + (x0), tmp12, xmask)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(2, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "xnumel": 4,
 "r0_numel": 1
}
```

Base-pointer alignment class `aligned`, element offsets per pointer argument:
`[0, 0, 0]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 8,
 "num_ctas": 1,
 "num_stages": 7,
 "maxnreg": null,
 "default_dot_input_precision": "tf32",
 "launch_cooperative_grid": true,
 "launch_pdl": false,
 "sanitize_overflow": false,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null
 },
 {
  "name": "out_ptr1",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr2",
  "kind": "ptr",
  "ty": "*i1",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 4
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 1
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 2
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
--tritongpu-coalesce
```
candidate (same, plus the culprit):
```
--tritongpu-coalesce \
      --tritongpu-remove-layout-conversions
```

## IR

* `artifacts/HIT-0146/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0146/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0146/output.ttgir` -- after the reference pipeline + `--tritongpu-remove-layout-conversions`
* `artifacts/HIT-0146/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [1, 8], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0]}>
@@ -3,5 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [8], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [0, 1]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [1, 8], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0]}>
-#blocked6 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [2, 16], warpsPerCTA = [1, 8], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [2, 16], warpsPerCTA = [1, 8], order = [0, 1]}>
@@ -10,6 +6,4 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<2x1024xf16, #blocked>
-    %cst_0 = arith.constant dense<0xFF800000> : tensor<2x1024xf32, #blocked>
-    %cst_1 = arith.constant dense<1.024000e+03> : tensor<2x1xf32, #blocked1>
-    %cst_2 = arith.constant dense<0.000000e+00> : tensor<2x1024xf32, #blocked>
-    %cst_3 = arith.constant dense<1024> : tensor<2x1xi32, #blocked1>
-    %cst_4 = arith.constant dense<4> : tensor<2x1xi32, #blocked1>
+    %cst = arith.constant dense<0.000000e+00> : tensor<2x1024xf32, #blocked>
+    %cst_0 = arith.constant dense<1.024000e+03> : tensor<2x1xf32, #blocked>
+    %cst_1 = arith.constant dense<0xFF800000> : tensor<2x1024xf32, #blocked>
+    %cst_2 = arith.constant dense<0.000000e+00> : tensor<2x1024xf16, #blocked>
@@ -16,0 +11,3 @@
+    %cst_3 = arith.constant dense<4> : tensor<2x1xi32, #blocked1>
+    %cst_4 = arith.constant dense<4> : tensor<2x1xi32, #blocked>
+    %cst_5 = arith.constant dense<1024> : tensor<2x1xi32, #blocked>
@@ -19,31 +16,28 @@
-    %2 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<2xi32, #blocked2> -> tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<2x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<2x1xi32, #blocked3> -> tensor<2x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<2x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<2x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_4 : tensor<2x1xi32, #blocked1>
-    %9 = tt.make_range {end = 1024 : i32, start = 0 : i32} : tensor<1024xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<1024xi32, #blocked2> -> tensor<1024xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<1024xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x1024xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x1024xi32, #blocked4> -> tensor<1x1024xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x1024xi32, #blocked4> -> tensor<1x1024xi32, #blocked>
-    %14 = arith.muli %7, %cst_3 : tensor<2x1xi32, #blocked1>
-    %15 = tt.broadcast %13 : tensor<1x1024xi32, #blocked> -> tensor<2x1024xi32, #blocked>
-    %16 = tt.broadcast %14 : tensor<2x1xi32, #blocked1> -> tensor<2x1024xi32, #blocked1>
-    %17 = ttg.convert_layout %16 : tensor<2x1024xi32, #blocked1> -> tensor<2x1024xi32, #blocked>
-    %18 = arith.addi %15, %17 : tensor<2x1024xi32, #blocked>
-    %19 = tt.splat %arg0 : !tt.ptr<f16> -> tensor<2x1024x!tt.ptr<f16>, #blocked>
-    %20 = tt.addptr %19, %18 : tensor<2x1024x!tt.ptr<f16>, #blocked>, tensor<2x1024xi32, #blocked>
-    %21 = tt.broadcast %8 : tensor<2x1xi1, #blocked1> -> tensor<2x1024xi1, #blocked1>
-    %22 = ttg.convert_layout %21 : tensor<2x1024xi1, #blocked1> -> tensor<2x1024xi1, #blocked>
-    %23 = ttg.convert_layout %20 : tensor<2x1024x!tt.ptr<f16>, #blocked> -> tensor<2x1024x!tt.ptr<f16>, #blocked5>
-    %24 = ttg.convert_layout %22 : tensor<2x1024xi1, #blocked> -> tensor<2x1024xi1, #blocked5>
-    %25 = ttg.convert_layout %cst : tensor<2x1024xf16, #blocked> -> tensor<2x1024xf16, #blocked5>
-    %26 = tt.load %23, %24, %25 evictionPolicy = evict_first : tensor<2x1024x!tt.ptr<f16>, #blocked5>
-    %27 = ttg.convert_layout %26 : tensor<2x1024xf16, #blocked5> -> tensor<2x1024xf16, #blocked>
-    %28 = arith.extf %27 : tensor<2x1024xf16, #blocked> to tensor<2x1024xf32, #blocked>
-    %29 = tt.broadcast %8 : tensor<2x1xi1, #blocked1> -> tensor<2x1024xi1, #blocked1>
-    %30 = ttg.convert_layout %29 : tensor<2x1024xi1, #blocked1> -> tensor<2x1024xi1, #blocked>
-    %31 = arith.select %30, %28, %cst_2 : tensor<2x1024xi1, #blocked>, tensor<2x1024xf32, #blocked>
-    %32 = "tt.reduce"(%31) <{axis = 1 : i32}> ({
+    %2 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked}>>
+    %3 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked}>>
+    %4 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked1}>>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 14**: fill mode `tiny`, fill seed `24103006`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `393223` (slot `1`, i.e. pointer
argument index `1`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 14,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "tiny",
 "secs": 0.073,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 1,
   "dtype": "torch.float32",
   "n": 32768,
   "ndiff": 3,
   "ndiff_ref_unwritten": 0,
   "ndiff_cand_unwritten": 0,
   "first_index": 1,
   "max_abs_diff": 0.0,
   "max_abs_value": 4.046768136886575e-30,
   "ref_value": -0.0,
   "cand_value": 0.0,
   "ref_bits": "0x80000000",
   "cand_bits": "0x0",
   "max_ulp": 0,
   "ndiff_nan_payload": 0,
   "ndiff_value": 3,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0,
   "first_value_index": 1
  }
 ],
 "ndiff_total": 3,
 "n_total": 229376,
 "ndiff_value_total": 3,
 "ndiff_nan_payload_total": 0,
 "ndiff_ref_unwritten": 0,
 "ndiff_cand_unwritten": 0,
 "max_abs_diff": 0.0,
 "max_abs_value": 4.046768136886575e-30,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addf`, `arith.addi`, `arith.cmpf`, `arith.cmpi`, `arith.constant`, `arith.divf`, `arith.extf`, `arith.extui`, `arith.muli`, `arith.ori`, `arith.select`, `arith.subf`, `tt.addptr`, `tt.bitcast`, `tt.broadcast`, `tt.expand_dims`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: `blocked3`, `blocked4`, `blocked5`, `blocked6`

**Diff size**: +71 / -91 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(2, 1, 1)` (programs = 2). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [1, 8], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0]}>
@@ -3,5 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [8], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [0, 1]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [1, 8], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0]}>
-#blocked6 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [2, 16], warpsPerCTA = [1, 8], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [2, 16], warpsPerCTA = [1, 8], order = [0, 1]}>
@@ -10,6 +6,4 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<2x1024xf16, #blocked>
-    %cst_0 = arith.constant dense<0xFF800000> : tensor<2x1024xf32, #blocked>
-    %cst_1 = arith.constant dense<1.024000e+03> : tensor<2x1xf32, #blocked1>
-    %cst_2 = arith.constant dense<0.000000e+00> : tensor<2x1024xf32, #blocked>
-    %cst_3 = arith.constant dense<1024> : tensor<2x1xi32, #blocked1>
-    %cst_4 = arith.constant dense<4> : tensor<2x1xi32, #blocked1>
+    %cst = arith.constant dense<0.000000e+00> : tensor<2x1024xf32, #blocked>
+    %cst_0 = arith.constant dense<1.024000e+03> : tensor<2x1xf32, #blocked>
+    %cst_1 = arith.constant dense<0xFF800000> : tensor<2x1024xf32, #blocked>
+    %cst_2 = arith.constant dense<0.000000e+00> : tensor<2x1024xf16, #blocked>
@@ -16,0 +11,3 @@
+    %cst_3 = arith.constant dense<4> : tensor<2x1xi32, #blocked1>
+    %cst_4 = arith.constant dense<4> : tensor<2x1xi32, #blocked>
+    %cst_5 = arith.constant dense<1024> : tensor<2x1xi32, #blocked>
@@ -19,31 +16,28 @@
-    %2 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<2xi32, #blocked2> -> tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<2x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<2x1xi32, #blocked3> -> tensor<2x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<2x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<2x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_4 : tensor<2x1xi32, #blocked1>
-    %9 = tt.make_range {end = 1024 : i32, start = 0 : i32} : tensor<1024xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<1024xi32, #blocked2> -> tensor<1024xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<1024xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x1024xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x1024xi32, #blocked4> -> tensor<1x1024xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x1024xi32, #blocked4> -> tensor<1x1024xi32, #blocked>
-    %14 = arith.muli %7, %cst_3 : tensor<2x1xi32, #blocked1>
-    %15 = tt.broadcast %13 : tensor<1x1024xi32, #blocked> -> tensor<2x1024xi32, #blocked>
-    %16 = tt.broadcast %14 : tensor<2x1xi32, #blocked1> -> tensor<2x1024xi32, #blocked1>
-    %17 = ttg.convert_layout %16 : tensor<2x1024xi32, #blocked1> -> tensor<2x1024xi32, #blocked>
-    %18 = arith.addi %15, %17 : tensor<2x1024xi32, #blocked>
-    %19 = tt.splat %arg0 : !tt.ptr<f16> -> tensor<2x1024x!tt.ptr<f16>, #blocked>
-    %20 = tt.addptr %19, %18 : tensor<2x1024x!tt.ptr<f16>, #blocked>, tensor<2x1024xi32, #blocked>
-    %21 = tt.broadcast %8 : tensor<2x1xi1, #blocked1> -> tensor<2x1024xi1, #blocked1>
-    %22 = ttg.convert_layout %21 : tensor<2x1024xi1, #blocked1> -> tensor<2x1024xi1, #blocked>
-    %23 = ttg.convert_layout %20 : tensor<2x1024x!tt.ptr<f16>, #blocked> -> tensor<2x1024x!tt.ptr<f16>, #blocked5>
-    %24 = ttg.convert_layout %22 : tensor<2x1024xi1, #blocked> -> tensor<2x1024xi1, #blocked5>
-    %25 = ttg.convert_layout %cst : tensor<2x1024xf16, #blocked> -> tensor<2x1024xf16, #blocked5>
-    %26 = tt.load %23, %24, %25 evictionPolicy = evict_first : tensor<2x1024x!tt.ptr<f16>, #blocked5>
-    %27 = ttg.convert_layout %26 : tensor<2x1024xf16, #blocked5> -> tensor<2x1024xf16, #blocked>
-    %28 = arith.extf %27 : tensor<2x1024xf16, #blocked> to tensor<2x1024xf32, #blocked>
-    %29 = tt.broadcast %8 : tensor<2x1xi1, #blocked1> -> tensor<2x1024xi1, #blocked1>
-    %30 = ttg.convert_layout %29 : tensor<2x1024xi1, #blocked1> -> tensor<2x1024xi1, #blocked>
-    %31 = arith.select %30, %28, %cst_2 : tensor<2x1024xi1, #blocked>, tensor<2x1024xf32, #blocked>
-    %32 = "tt.reduce"(%31) <{axis = 1 : i32}> ({
+    %2 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked}>>
+    %3 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked}>>
+    %4 = tt.make_range {end = 2 : i32, start = 0 : i32} : tensor<2xi32, #ttg.slice<{dim = 1, parent = #blocked1}>>
```

