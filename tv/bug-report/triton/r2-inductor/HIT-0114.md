# HIT-0114: triton-nvidia-gpu-plan-cta on gen:triton_per_fused__to_copy_abs_add_atan2_neg_pow_std_2_4fa5bb1f
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--triton-nvidia-gpu-plan-cta`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=bf16x3, enable_fp_fusion=False, launch_cooperative_grid=False, launch_pdl=False, maxnreg=64, num_ctas=2, num_stages=7, num_warps=8, sanitize_overflow=True
* base-pointer alignment: aligned (element offsets [0, 0, 0])
* shape draw: numels={'xnumel': 1, 'r0_numel': 37} blocks={'XBLOCK': 16, 'xnumel': 1} grid=[1, 1, 1]
* layout-reorders-reduction
* ndiff/total: 1/163840
* of those, real value differences: 0; NaN-payload-only: 1
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0114
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0114/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0114/output.ttgir
triton-opt artifacts/HIT-0114/input.ttgir --triton-nvidia-gpu-plan-cta -o /tmp/HIT-0114.ttgir
diff artifacts/HIT-0114/output.ttgir /tmp/HIT-0114.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **1580**  (`python -m fz.progen 1580` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_per_fused__to_copy_abs_add_atan2_neg_pow_std_2`, heuristic `persistent_reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": false,
 "max_autotune": true,
 "persistent_reductions": null,
 "cooperative_reductions": null,
 "multi_kernel": null,
 "combo_kernels": true,
 "prefer_nd_tiling": null,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": null,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0114/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 12512020
import torch
import torch.nn.functional as F

SEED = 12512020
KNOBS = {'dynamic': False, 'backward': False, 'max_autotune': True, 'persistent_reductions': None, 'cooperative_reductions': None, 'multi_kernel': None, 'combo_kernels': True, 'prefer_nd_tiling': None, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': None, 'benchmark_kernel': False}
BACKWARD = False


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(12512020)
    a0 = torch.randn((65, ), device=device, dtype=torch.float32, generator=g).to(torch.float16)
    a1 = torch.randn((65, ), device=device, dtype=torch.float32, generator=g).to(torch.bfloat16)
    c0 = torch.randint(0, 65, (65, ), device=device, dtype=torch.int64, generator=g)
    return [a0, a1, c0]


def prog(a0, a1, c0):
    v1 = torch.cumsum(a1.float(), dim=0)
    v2 = torch.minimum(a0, a0)
    v3 = torch.index_select(a1, 0, c0)
    v4 = torch.log(a0.abs() + 1.0)
    v5 = torch.var(a0.float(), dim=0, keepdim=True)
    v6 = torch.cat([v3, a1], dim=0)
    v7 = torch.atan2(a1, a1 + 1.0)
    v8 = torch.pow(v6.abs() + 0.5, 2)
    v9 = torch.atan2(v6, v8 + 1.0)
    v10 = torch.addcmul(a1, a1, v1, value=0.5)
    v11 = v8 * v8 + v8
    v12 = torch.amax(v11, dim=0, keepdim=True)
    v13 = torch.sort(v7, dim=0)[0]
    v14 = -v9
    v15 = torch.std(v6.float(), dim=0, keepdim=False)
    return (v13, v14, v15)

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0114/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 1580
# source file  : ccunotlhuvhkji6znsbyg5wobk2c45xp24csdvwk2wwr2k343wrk.py
# inductor name: triton_per_fused__to_copy_abs_add_atan2_neg_pow_std_2
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_per_fused__to_copy_abs_add_atan2_neg_pow_std_2_4fa5bb1f(in_out_ptr0, in_ptr0, out_ptr0, xnumel, r0_numel, XBLOCK : tl.constexpr):
    xnumel = 1
    r0_numel = 37
    R0_BLOCK: tl.constexpr = 256
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = tl.full([XBLOCK], True, tl.int1)[:, None]
    r0_index = tl.arange(0, R0_BLOCK)[None, :]
    r0_offset = 0
    r0_mask = r0_index < r0_numel
    roffset = r0_offset
    rindex = r0_index
    r0_0 = r0_index
    tmp0 = tl.load(in_ptr0 + (r0_0), r0_mask, eviction_policy='evict_first', other=0.0).to(tl.float32)
    tmp1 = tl_math.abs(tmp0)
    tmp2 = tl.full([1, 1], 0.5, tl.float32)
    tmp3 = tmp1 + tmp2
    tmp4 = tmp3 * tmp3
    tmp5 = tl.full([1, 1], 1.0, tl.float32)
    tmp6 = tmp4 + tmp5
    tmp7 = libdevice.atan2(tmp0, tmp6)
    tmp8 = -tmp7
    tmp9 = tmp0.to(tl.float32)
    tmp10 = tl.broadcast_to(tmp9, [XBLOCK, R0_BLOCK])
    tmp12 = tl.where(r0_mask, tmp10, 0)
    tmp13 = tl.broadcast_to(tmp10, [XBLOCK, R0_BLOCK])
    tmp15 = tl.where(r0_mask, tmp13, 0)
    tmp16 = tl.sum(tmp15, 1)[:, None].to(tl.float32)
    tmp17 = (tl.full([1, 1], 130, tl.int32)).to(tl.float32)
    tmp18 = (tmp16 / tmp17)
    tmp19 = tmp10 - tmp18
    tmp20 = tmp19 * tmp19
    tmp21 = tl.broadcast_to(tmp20, [XBLOCK, R0_BLOCK])
    tmp23 = tl.where(r0_mask, tmp21, 0)
    tmp24 = tl.sum(tmp23, 1)[:, None].to(tl.float32)
    tmp25 = tl.full([1, 1], 129.0, tl.float32)
    tmp26 = (tmp24 / tmp25)
    tmp27 = tl.sqrt_rn(tmp26)
    tl.store(out_ptr0 + (tl.broadcast_to(r0_0, [XBLOCK, R0_BLOCK])), tmp8, r0_mask)
    tl.store(in_out_ptr0 + (tl.full([1, 1], 0, tl.int32).broadcast_to(XBLOCK, 1)), tmp27, None)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(1, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "r0_numel": 37
}
```

Base-pointer alignment class `aligned`, element offsets per pointer argument:
`[0, 0, 0]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 8,
 "num_ctas": 2,
 "num_stages": 7,
 "maxnreg": 64,
 "default_dot_input_precision": "bf16x3",
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": true,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*bf16",
  "value": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*bf16",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 1
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 37
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 16
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--triton-nvidia-gpu-plan-cta
```

## IR

* `artifacts/HIT-0114/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0114/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0114/output.ttgir` -- after the reference pipeline + `--triton-nvidia-gpu-plan-cta`
* `artifacts/HIT-0114/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -1,6 +1,4 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [1, 0], CGALayout = [[1, 0]]}>
-#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0], CGALayout = [[0, 1]]}>
-#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0], CGALayout = [[1, 0]]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [8], order = [0], CGALayout = [[1]]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [0, 1], CGALayout = [[0, 1]]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1], CGALayout = [[1, 0]]}>
+#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1], CGALayout = [[1, 0]]}>
+#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0], CGALayout = [[1, 0]]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0], CGALayout = [[0, 1]]}>
+#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [1, 0], CGALayout = [[1, 0]]}>
@@ -11,28 +9,30 @@
-    %cst_1 = arith.constant dense<1.290000e+02> : tensor<16x1xf32, #blocked>
-    %cst_2 = arith.constant dense<0.000000e+00> : tensor<16x256xf32, #blocked2>
-    %cst_3 = arith.constant dense<1.000000e+00> : tensor<1x256xf32, #blocked1>
-    %cst_4 = arith.constant dense<5.000000e-01> : tensor<1x256xf32, #blocked1>
-    %cst_5 = arith.constant dense<37> : tensor<1x256xi32, #blocked1>
-    %0 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #blocked3>
-    %1 = ttg.convert_layout %0 : tensor<256xi32, #blocked3> -> tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x256xi32, #blocked4>
-    %3 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %4 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %5 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %6 = arith.cmpi slt, %5, %cst_5 : tensor<1x256xi32, #blocked1>
-    %7 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked1>
-    %8 = tt.addptr %7, %4 : tensor<1x256x!tt.ptr<bf16>, #blocked1>, tensor<1x256xi32, #blocked1>
-    %9 = tt.load %8, %6, %cst_0 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked1>
-    %10 = arith.extf %9 : tensor<1x256xbf16, #blocked1> to tensor<1x256xf32, #blocked1>
-    %11 = math.absf %10 : tensor<1x256xf32, #blocked1>
-    %12 = arith.addf %11, %cst_4 : tensor<1x256xf32, #blocked1>
-    %13 = arith.mulf %12, %12 : tensor<1x256xf32, #blocked1>
-    %14 = arith.addf %13, %cst_3 : tensor<1x256xf32, #blocked1>
-    %15 = tt.extern_elementwise %10, %14 {libname = "", libpath = "", pure = true, symbol = "__nv_atan2f"} : (tensor<1x256xf32, #blocked1>, tensor<1x256xf32, #blocked1>) -> tensor<1x256xf32, #blocked1>
-    %16 = arith.negf %15 : tensor<1x256xf32, #blocked1>
-    %17 = tt.broadcast %10 : tensor<1x256xf32, #blocked1> -> tensor<16x256xf32, #blocked1>
-    %18 = ttg.convert_layout %17 : tensor<16x256xf32, #blocked1> -> tensor<16x256xf32, #blocked2>
-    %19 = ttg.convert_layout %17 : tensor<16x256xf32, #blocked1> -> tensor<16x256xf32, #blocked2>
-    %20 = tt.broadcast %6 : tensor<1x256xi1, #blocked1> -> tensor<16x256xi1, #blocked1>
-    %21 = ttg.convert_layout %20 : tensor<16x256xi1, #blocked1> -> tensor<16x256xi1, #blocked2>
-    %22 = arith.select %21, %19, %cst_2 : tensor<16x256xi1, #blocked2>, tensor<16x256xf32, #blocked2>
+    %cst_1 = arith.constant dense<0.000000e+00> : tensor<1x256xbf16, #blocked2>
+    %cst_2 = arith.constant dense<1.290000e+02> : tensor<16x1xf32, #blocked>
+    %cst_3 = arith.constant dense<0.000000e+00> : tensor<16x256xf32, #blocked1>
+    %cst_4 = arith.constant dense<1.000000e+00> : tensor<1x256xf32, #blocked2>
+    %cst_5 = arith.constant dense<5.000000e-01> : tensor<1x256xf32, #blocked2>
+    %cst_6 = arith.constant dense<37> : tensor<1x256xi32, #blocked2>
+    %cst_7 = arith.constant dense<37> : tensor<1x256xi32, #blocked1>
+    %0 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked1}>>
+    %1 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked2}>>
+    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked2}>> -> tensor<1x256xi32, #blocked2>
+    %3 = tt.expand_dims %0 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked1}>> -> tensor<1x256xi32, #blocked1>
+    %4 = arith.cmpi slt, %2, %cst_6 : tensor<1x256xi32, #blocked2>
+    %5 = arith.cmpi slt, %3, %cst_7 : tensor<1x256xi32, #blocked1>
+    %6 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked1>
+    %7 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked2>
+    %8 = tt.addptr %6, %3 : tensor<1x256x!tt.ptr<bf16>, #blocked1>, tensor<1x256xi32, #blocked1>
+    %9 = tt.addptr %7, %2 : tensor<1x256x!tt.ptr<bf16>, #blocked2>, tensor<1x256xi32, #blocked2>
+    %10 = tt.load %8, %5, %cst_0 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked1>
+    %11 = tt.load %9, %4, %cst_1 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked2>
+    %12 = arith.extf %10 : tensor<1x256xbf16, #blocked1> to tensor<1x256xf32, #blocked1>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 5**: fill mode `extreme`, fill seed `23970818`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `524357` (slot `2`, i.e. pointer
argument index `2`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 5,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "extreme",
 "secs": 0.019,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 2,
   "dtype": "torch.bfloat16",
   "n": 65536,
   "ndiff": 1,
   "ndiff_ref_unwritten": 0,
   "ndiff_cand_unwritten": 0,
   "first_index": 34,
   "max_abs_diff": 0.0,
   "max_abs_value": 9.969209968386869e+37,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0x7fff",
   "cand_bits": "0xffff",
   "max_ulp": 0,
   "ndiff_nan_payload": 1,
   "ndiff_value": 0,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0
  }
 ],
 "ndiff_total": 1,
 "n_total": 163840,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 1,
 "ndiff_ref_unwritten": 0,
 "ndiff_cand_unwritten": 0,
 "max_abs_diff": 0.0,
 "max_abs_value": 9.969209968386869e+37,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addf`, `arith.cmpi`, `arith.constant`, `arith.divf`, `arith.extf`, `arith.mulf`, `arith.negf`, `arith.select`, `arith.subf`, `arith.truncf`, `math.absf`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.extern_elementwise`, `tt.load`, `tt.make_range`, `tt.precise_sqrt`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: `blocked4`, `blocked5`

**Diff size**: +66 / -71 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(1, 1, 1)` (programs = 1). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -1,6 +1,4 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [1, 0], CGALayout = [[1, 0]]}>
-#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0], CGALayout = [[0, 1]]}>
-#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0], CGALayout = [[1, 0]]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [8], order = [0], CGALayout = [[1]]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [0, 1], CGALayout = [[0, 1]]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1], CGALayout = [[1, 0]]}>
+#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [0, 1], CGALayout = [[1, 0]]}>
+#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 8], order = [1, 0], CGALayout = [[1, 0]]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [2, 4], order = [1, 0], CGALayout = [[0, 1]]}>
+#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [8, 1], order = [1, 0], CGALayout = [[1, 0]]}>
@@ -11,28 +9,30 @@
-    %cst_1 = arith.constant dense<1.290000e+02> : tensor<16x1xf32, #blocked>
-    %cst_2 = arith.constant dense<0.000000e+00> : tensor<16x256xf32, #blocked2>
-    %cst_3 = arith.constant dense<1.000000e+00> : tensor<1x256xf32, #blocked1>
-    %cst_4 = arith.constant dense<5.000000e-01> : tensor<1x256xf32, #blocked1>
-    %cst_5 = arith.constant dense<37> : tensor<1x256xi32, #blocked1>
-    %0 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #blocked3>
-    %1 = ttg.convert_layout %0 : tensor<256xi32, #blocked3> -> tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x256xi32, #blocked4>
-    %3 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %4 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %5 = ttg.convert_layout %2 : tensor<1x256xi32, #blocked4> -> tensor<1x256xi32, #blocked1>
-    %6 = arith.cmpi slt, %5, %cst_5 : tensor<1x256xi32, #blocked1>
-    %7 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked1>
-    %8 = tt.addptr %7, %4 : tensor<1x256x!tt.ptr<bf16>, #blocked1>, tensor<1x256xi32, #blocked1>
-    %9 = tt.load %8, %6, %cst_0 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked1>
-    %10 = arith.extf %9 : tensor<1x256xbf16, #blocked1> to tensor<1x256xf32, #blocked1>
-    %11 = math.absf %10 : tensor<1x256xf32, #blocked1>
-    %12 = arith.addf %11, %cst_4 : tensor<1x256xf32, #blocked1>
-    %13 = arith.mulf %12, %12 : tensor<1x256xf32, #blocked1>
-    %14 = arith.addf %13, %cst_3 : tensor<1x256xf32, #blocked1>
-    %15 = tt.extern_elementwise %10, %14 {libname = "", libpath = "", pure = true, symbol = "__nv_atan2f"} : (tensor<1x256xf32, #blocked1>, tensor<1x256xf32, #blocked1>) -> tensor<1x256xf32, #blocked1>
-    %16 = arith.negf %15 : tensor<1x256xf32, #blocked1>
-    %17 = tt.broadcast %10 : tensor<1x256xf32, #blocked1> -> tensor<16x256xf32, #blocked1>
-    %18 = ttg.convert_layout %17 : tensor<16x256xf32, #blocked1> -> tensor<16x256xf32, #blocked2>
-    %19 = ttg.convert_layout %17 : tensor<16x256xf32, #blocked1> -> tensor<16x256xf32, #blocked2>
-    %20 = tt.broadcast %6 : tensor<1x256xi1, #blocked1> -> tensor<16x256xi1, #blocked1>
-    %21 = ttg.convert_layout %20 : tensor<16x256xi1, #blocked1> -> tensor<16x256xi1, #blocked2>
-    %22 = arith.select %21, %19, %cst_2 : tensor<16x256xi1, #blocked2>, tensor<16x256xf32, #blocked2>
+    %cst_1 = arith.constant dense<0.000000e+00> : tensor<1x256xbf16, #blocked2>
+    %cst_2 = arith.constant dense<1.290000e+02> : tensor<16x1xf32, #blocked>
+    %cst_3 = arith.constant dense<0.000000e+00> : tensor<16x256xf32, #blocked1>
+    %cst_4 = arith.constant dense<1.000000e+00> : tensor<1x256xf32, #blocked2>
+    %cst_5 = arith.constant dense<5.000000e-01> : tensor<1x256xf32, #blocked2>
+    %cst_6 = arith.constant dense<37> : tensor<1x256xi32, #blocked2>
+    %cst_7 = arith.constant dense<37> : tensor<1x256xi32, #blocked1>
+    %0 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked1}>>
+    %1 = tt.make_range {end = 256 : i32, start = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked2}>>
+    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked2}>> -> tensor<1x256xi32, #blocked2>
+    %3 = tt.expand_dims %0 {axis = 0 : i32} : tensor<256xi32, #ttg.slice<{dim = 0, parent = #blocked1}>> -> tensor<1x256xi32, #blocked1>
+    %4 = arith.cmpi slt, %2, %cst_6 : tensor<1x256xi32, #blocked2>
+    %5 = arith.cmpi slt, %3, %cst_7 : tensor<1x256xi32, #blocked1>
+    %6 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked1>
+    %7 = tt.splat %arg1 : !tt.ptr<bf16> -> tensor<1x256x!tt.ptr<bf16>, #blocked2>
+    %8 = tt.addptr %6, %3 : tensor<1x256x!tt.ptr<bf16>, #blocked1>, tensor<1x256xi32, #blocked1>
+    %9 = tt.addptr %7, %2 : tensor<1x256x!tt.ptr<bf16>, #blocked2>, tensor<1x256xi32, #blocked2>
+    %10 = tt.load %8, %5, %cst_0 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked1>
+    %11 = tt.load %9, %4, %cst_1 evictionPolicy = evict_first : tensor<1x256x!tt.ptr<bf16>, #blocked2>
+    %12 = arith.extf %10 : tensor<1x256xbf16, #blocked1> to tensor<1x256xf32, #blocked1>
```

