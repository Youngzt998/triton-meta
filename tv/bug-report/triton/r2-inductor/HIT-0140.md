# HIT-0140: tritongpu-remove-layout-conversions on gen:triton_red_fused_amin_prod_unsqueeze_0_7e5f3abd
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--tritongpu-remove-layout-conversions`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=bf16x6, enable_fp_fusion=False, launch_cooperative_grid=True, launch_pdl=True, maxnreg=96, num_ctas=1, num_stages=1, num_warps=4, sanitize_overflow=False
* base-pointer alignment: byte (element offsets [2, 3, 3])
* shape draw: numels={'xnumel': 1, 'r0_numel': 17} blocks={'XBLOCK': 128, 'R0_BLOCK': 32, 'xnumel': 1} grid=[1, 1, 1]
* layout-reorders-reduction
* ndiff/total: 1/98304
* of those, real value differences: 0; NaN-payload-only: 1
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0140
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0140/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0140/output.ttgir
triton-opt artifacts/HIT-0140/input.ttgir --tritongpu-remove-layout-conversions -o /tmp/HIT-0140.ttgir
diff artifacts/HIT-0140/output.ttgir /tmp/HIT-0140.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **2751**  (`python -m fz.progen 2751` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_red_fused_amin_prod_unsqueeze_0`, heuristic `reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": false,
 "max_autotune": false,
 "persistent_reductions": true,
 "cooperative_reductions": true,
 "multi_kernel": 1,
 "combo_kernels": null,
 "prefer_nd_tiling": null,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": null,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0140/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 21785169
import torch
import torch.nn.functional as F

SEED = 21785169
KNOBS = {'dynamic': False, 'backward': False, 'max_autotune': False, 'persistent_reductions': True, 'cooperative_reductions': True, 'multi_kernel': 1, 'combo_kernels': None, 'prefer_nd_tiling': None, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': None, 'benchmark_kernel': False}
BACKWARD = False


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(21785169)
    a0 = torch.randn((71, ), device=device, dtype=torch.float32, generator=g).to(torch.float16)
    a1 = torch.randn((71, ), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    c0 = torch.randn((71, 3), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    c1 = torch.randn((3, ), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    return [a0, a1, c0, c1]


def prog(a0, a1, c0, c1):
    v1 = torch.exp(a1)
    v2 = torch.cat([a1, v1], dim=0)
    v3 = a1.unsqueeze(0)
    v4 = v1.narrow(0, 20, 50)
    v5 = v3 * 0.1849
    v6 = F.hardtanh(a0)
    v7 = v2 * v2
    v8 = torch.addmm(c1, v3, c0)
    v9 = (c0 < c1)
    v10 = torch.where(v9, c0, c1)
    v11 = torch.ceil(c0)
    v12 = (v8.float() - v8.float().mean(-1, keepdim=True))
    v13 = torch.clamp(v10, -3.0, 3.0)
    v14 = torch.prod(v3, dim=0, keepdim=False)
    v15 = torch.amin(v3, dim=1, keepdim=True)
    return (v14, v15)

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0140/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 2751
# source file  : cjcc7no7csnti7nj5yhlat3uga3kdvxakcg4naflzwjr4fap5x4c.py
# inductor name: triton_red_fused_amin_prod_unsqueeze_0
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_red_fused_amin_prod_unsqueeze_0_7e5f3abd(in_ptr0, out_ptr0, out_ptr1, xnumel, r0_numel, XBLOCK : tl.constexpr, R0_BLOCK : tl.constexpr):
    xnumel = 1
    r0_numel = 17
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = tl.full([XBLOCK], True, tl.int1)[:, None]
    r0_base = tl.arange(0, R0_BLOCK)[None, :]
    rbase = r0_base
    _tmp2 = tl.full([XBLOCK, R0_BLOCK], float("inf"), tl.float32)
    for r0_offset in tl.range(0, r0_numel, R0_BLOCK):
        r0_index = r0_offset + r0_base
        r0_mask = r0_index < r0_numel
        roffset = r0_offset
        rindex = r0_index
        r0_0 = r0_index
        tmp0 = tl.load(in_ptr0 + (r0_0), r0_mask, eviction_policy='evict_first', other=0.0)
        tmp1 = tl.broadcast_to(tmp0, [XBLOCK, R0_BLOCK])
        tmp3 = triton_helpers.minimum(_tmp2, tmp1)
        _tmp2 = tl.where(r0_mask, tmp3, _tmp2)
        tl.store(out_ptr0 + (tl.broadcast_to(r0_0, [XBLOCK, R0_BLOCK])), tmp0, r0_mask)
    tmp2 = triton_helpers.min2(_tmp2, 1)[:, None]
    tl.store(out_ptr1 + (tl.full([1, 1], 0, tl.int32).broadcast_to(XBLOCK, 1)), tmp2, None)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(1, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "r0_numel": 17
}
```

Base-pointer alignment class `byte`, element offsets per pointer argument:
`[2, 3, 3]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 4,
 "num_ctas": 1,
 "num_stages": 1,
 "maxnreg": 96,
 "default_dot_input_precision": "bf16x6",
 "launch_cooperative_grid": true,
 "launch_pdl": true,
 "sanitize_overflow": false,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr1",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 1
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 17
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 128
 },
 {
  "name": "R0_BLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 32
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--tritongpu-remove-layout-conversions
```

## IR

* `artifacts/HIT-0140/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0140/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0140/output.ttgir` -- after the reference pipeline + `--tritongpu-remove-layout-conversions`
* `artifacts/HIT-0140/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -2,4 +2 @@
-#blocked1 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [4], order = [0]}>
-#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [0, 1]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [1, 0]}>
+#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [1, 0]}>
@@ -8,10 +5,12 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked>
-    %cst_0 = arith.constant dense<17> : tensor<1x32xi32, #blocked>
-    %cst_1 = arith.constant dense<0x7F800000> : tensor<128x32xf32, #blocked>
-    %0 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #blocked1>
-    %1 = ttg.convert_layout %0 : tensor<32xi32, #blocked1> -> tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked2}>>
-    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked2}>> -> tensor<1x32xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %4 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %5 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %6 = arith.cmpi slt, %5, %cst_0 : tensor<1x32xi32, #blocked>
+    %cst = arith.constant dense<17> : tensor<1x32xi32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked>
+    %cst_1 = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked1>
+    %cst_2 = arith.constant dense<17> : tensor<1x32xi32, #blocked1>
+    %cst_3 = arith.constant dense<0x7F800000> : tensor<128x32xf32, #blocked1>
+    %0 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked1}>>
+    %1 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked}>>
+    %2 = tt.expand_dims %0 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked1}>> -> tensor<1x32xi32, #blocked1>
+    %3 = tt.expand_dims %1 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked}>> -> tensor<1x32xi32, #blocked>
+    %4 = arith.cmpi slt, %2, %cst_2 : tensor<1x32xi32, #blocked1>
+    %5 = arith.cmpi slt, %3, %cst : tensor<1x32xi32, #blocked>
+    %6 = tt.splat %arg0 : !tt.ptr<f32> -> tensor<1x32x!tt.ptr<f32>, #blocked1>
@@ -19,16 +18,18 @@
-    %8 = tt.addptr %7, %4 : tensor<1x32x!tt.ptr<f32>, #blocked>, tensor<1x32xi32, #blocked>
-    %9 = tt.load %8, %6, %cst evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked>
-    %10 = tt.broadcast %9 : tensor<1x32xf32, #blocked> -> tensor<128x32xf32, #blocked>
-    %11 = arith.cmpf olt, %cst_1, %10 : tensor<128x32xf32, #blocked>
-    %12 = arith.cmpf une, %cst_1, %cst_1 : tensor<128x32xf32, #blocked>
-    %13 = arith.ori %11, %12 : tensor<128x32xi1, #blocked>
-    %14 = arith.select %13, %cst_1, %10 : tensor<128x32xi1, #blocked>, tensor<128x32xf32, #blocked>
-    %15 = tt.broadcast %6 : tensor<1x32xi1, #blocked> -> tensor<128x32xi1, #blocked>
-    %16 = arith.select %15, %14, %cst_1 : tensor<128x32xi1, #blocked>, tensor<128x32xf32, #blocked>
-    %17 = tt.broadcast %3 : tensor<1x32xi32, #blocked> -> tensor<128x32xi32, #blocked>
-    %18 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<128x32x!tt.ptr<f32>, #blocked>
-    %19 = tt.addptr %18, %17 : tensor<128x32x!tt.ptr<f32>, #blocked>, tensor<128x32xi32, #blocked>
-    %20 = tt.broadcast %9 : tensor<1x32xf32, #blocked> -> tensor<128x32xf32, #blocked>
-    %21 = tt.broadcast %6 : tensor<1x32xi1, #blocked> -> tensor<128x32xi1, #blocked>
-    tt.store %19, %20, %21 : tensor<128x32x!tt.ptr<f32>, #blocked>
-    %22 = "tt.reduce"(%16) <{axis = 1 : i32}> ({
+    %8 = tt.addptr %6, %2 : tensor<1x32x!tt.ptr<f32>, #blocked1>, tensor<1x32xi32, #blocked1>
+    %9 = tt.addptr %7, %3 : tensor<1x32x!tt.ptr<f32>, #blocked>, tensor<1x32xi32, #blocked>
+    %10 = tt.load %8, %4, %cst_1 evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked1>
+    %11 = tt.load %9, %5, %cst_0 evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked>
+    %12 = tt.broadcast %10 : tensor<1x32xf32, #blocked1> -> tensor<128x32xf32, #blocked1>
+    %13 = arith.cmpf olt, %cst_3, %12 : tensor<128x32xf32, #blocked1>
+    %14 = arith.cmpf une, %cst_3, %cst_3 : tensor<128x32xf32, #blocked1>
+    %15 = arith.ori %13, %14 : tensor<128x32xi1, #blocked1>
+    %16 = arith.select %15, %cst_3, %12 : tensor<128x32xi1, #blocked1>, tensor<128x32xf32, #blocked1>
+    %17 = tt.broadcast %4 : tensor<1x32xi1, #blocked1> -> tensor<128x32xi1, #blocked1>
+    %18 = arith.select %17, %16, %cst_3 : tensor<128x32xi1, #blocked1>, tensor<128x32xf32, #blocked1>
+    %19 = tt.broadcast %3 : tensor<1x32xi32, #blocked> -> tensor<128x32xi32, #blocked>
+    %20 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<128x32x!tt.ptr<f32>, #blocked>
+    %21 = tt.addptr %20, %19 : tensor<128x32x!tt.ptr<f32>, #blocked>, tensor<128x32xi32, #blocked>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 7**: fill mode `nanpayload`, fill seed `24235178`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `524300` (slot `2`, i.e. pointer
argument index `2`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 7,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "nanpayload",
 "secs": 0.031,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 2,
   "dtype": "torch.float32",
   "n": 32768,
   "ndiff": 1,
   "ndiff_ref_unwritten": 0,
   "ndiff_cand_unwritten": 0,
   "first_index": 3,
   "max_abs_diff": 0.0,
   "max_abs_value": 0.0,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0x7feb90a8",
   "cand_bits": "0x7ffdcfbe",
   "max_ulp": 0,
   "ndiff_nan_payload": 1,
   "ndiff_value": 0,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0
  }
 ],
 "ndiff_total": 1,
 "n_total": 98304,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 1,
 "ndiff_ref_unwritten": 0,
 "ndiff_cand_unwritten": 0,
 "max_abs_diff": 0.0,
 "max_abs_value": 0.0,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: `ttg.convert_layout`
* all ops touched by the diff: `arith.cmpf`, `arith.cmpi`, `arith.constant`, `arith.ori`, `arith.select`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`, `ttg.convert_layout`, `ttg.slice`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: `blocked2`, `blocked3`, `blocked4`

**Diff size**: +40 / -42 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(1, 1, 1)` (programs = 1). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -2,4 +2 @@
-#blocked1 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [4], order = [0]}>
-#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 4], order = [0, 1]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [1, 0]}>
+#blocked1 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [4, 1], order = [1, 0]}>
@@ -8,10 +5,12 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked>
-    %cst_0 = arith.constant dense<17> : tensor<1x32xi32, #blocked>
-    %cst_1 = arith.constant dense<0x7F800000> : tensor<128x32xf32, #blocked>
-    %0 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #blocked1>
-    %1 = ttg.convert_layout %0 : tensor<32xi32, #blocked1> -> tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked2}>>
-    %2 = tt.expand_dims %1 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked2}>> -> tensor<1x32xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %4 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %5 = ttg.convert_layout %2 : tensor<1x32xi32, #blocked2> -> tensor<1x32xi32, #blocked>
-    %6 = arith.cmpi slt, %5, %cst_0 : tensor<1x32xi32, #blocked>
+    %cst = arith.constant dense<17> : tensor<1x32xi32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked>
+    %cst_1 = arith.constant dense<0.000000e+00> : tensor<1x32xf32, #blocked1>
+    %cst_2 = arith.constant dense<17> : tensor<1x32xi32, #blocked1>
+    %cst_3 = arith.constant dense<0x7F800000> : tensor<128x32xf32, #blocked1>
+    %0 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked1}>>
+    %1 = tt.make_range {end = 32 : i32, start = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked}>>
+    %2 = tt.expand_dims %0 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked1}>> -> tensor<1x32xi32, #blocked1>
+    %3 = tt.expand_dims %1 {axis = 0 : i32} : tensor<32xi32, #ttg.slice<{dim = 0, parent = #blocked}>> -> tensor<1x32xi32, #blocked>
+    %4 = arith.cmpi slt, %2, %cst_2 : tensor<1x32xi32, #blocked1>
+    %5 = arith.cmpi slt, %3, %cst : tensor<1x32xi32, #blocked>
+    %6 = tt.splat %arg0 : !tt.ptr<f32> -> tensor<1x32x!tt.ptr<f32>, #blocked1>
@@ -19,16 +18,18 @@
-    %8 = tt.addptr %7, %4 : tensor<1x32x!tt.ptr<f32>, #blocked>, tensor<1x32xi32, #blocked>
-    %9 = tt.load %8, %6, %cst evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked>
-    %10 = tt.broadcast %9 : tensor<1x32xf32, #blocked> -> tensor<128x32xf32, #blocked>
-    %11 = arith.cmpf olt, %cst_1, %10 : tensor<128x32xf32, #blocked>
-    %12 = arith.cmpf une, %cst_1, %cst_1 : tensor<128x32xf32, #blocked>
-    %13 = arith.ori %11, %12 : tensor<128x32xi1, #blocked>
-    %14 = arith.select %13, %cst_1, %10 : tensor<128x32xi1, #blocked>, tensor<128x32xf32, #blocked>
-    %15 = tt.broadcast %6 : tensor<1x32xi1, #blocked> -> tensor<128x32xi1, #blocked>
-    %16 = arith.select %15, %14, %cst_1 : tensor<128x32xi1, #blocked>, tensor<128x32xf32, #blocked>
-    %17 = tt.broadcast %3 : tensor<1x32xi32, #blocked> -> tensor<128x32xi32, #blocked>
-    %18 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<128x32x!tt.ptr<f32>, #blocked>
-    %19 = tt.addptr %18, %17 : tensor<128x32x!tt.ptr<f32>, #blocked>, tensor<128x32xi32, #blocked>
-    %20 = tt.broadcast %9 : tensor<1x32xf32, #blocked> -> tensor<128x32xf32, #blocked>
-    %21 = tt.broadcast %6 : tensor<1x32xi1, #blocked> -> tensor<128x32xi1, #blocked>
-    tt.store %19, %20, %21 : tensor<128x32x!tt.ptr<f32>, #blocked>
-    %22 = "tt.reduce"(%16) <{axis = 1 : i32}> ({
+    %8 = tt.addptr %6, %2 : tensor<1x32x!tt.ptr<f32>, #blocked1>, tensor<1x32xi32, #blocked1>
+    %9 = tt.addptr %7, %3 : tensor<1x32x!tt.ptr<f32>, #blocked>, tensor<1x32xi32, #blocked>
+    %10 = tt.load %8, %4, %cst_1 evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked1>
+    %11 = tt.load %9, %5, %cst_0 evictionPolicy = evict_first : tensor<1x32x!tt.ptr<f32>, #blocked>
+    %12 = tt.broadcast %10 : tensor<1x32xf32, #blocked1> -> tensor<128x32xf32, #blocked1>
+    %13 = arith.cmpf olt, %cst_3, %12 : tensor<128x32xf32, #blocked1>
+    %14 = arith.cmpf une, %cst_3, %cst_3 : tensor<128x32xf32, #blocked1>
+    %15 = arith.ori %13, %14 : tensor<128x32xi1, #blocked1>
+    %16 = arith.select %15, %cst_3, %12 : tensor<128x32xi1, #blocked1>, tensor<128x32xf32, #blocked1>
+    %17 = tt.broadcast %4 : tensor<1x32xi1, #blocked1> -> tensor<128x32xi1, #blocked1>
+    %18 = arith.select %17, %16, %cst_3 : tensor<128x32xi1, #blocked1>, tensor<128x32xf32, #blocked1>
+    %19 = tt.broadcast %3 : tensor<1x32xi32, #blocked> -> tensor<128x32xi32, #blocked>
+    %20 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<128x32x!tt.ptr<f32>, #blocked>
+    %21 = tt.addptr %20, %19 : tensor<128x32x!tt.ptr<f32>, #blocked>, tensor<128x32xi32, #blocked>
```

