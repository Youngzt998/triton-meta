# HIT-0098: tritongpu-remove-layout-conversions on gen:triton_red_fused_amax_gt_where_1_2f917fc6
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--tritongpu-remove-layout-conversions`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=tf32, enable_fp_fusion=False, launch_cooperative_grid=False, launch_pdl=False, maxnreg=None, num_ctas=1, num_stages=8, num_warps=2, sanitize_overflow=True
* base-pointer alignment: elem (element offsets [0, 4])
* shape draw: numels={'xnumel': 64, 'r0_numel': 7} blocks={'XBLOCK': 64, 'R0_BLOCK': 8} grid=[1, 1, 1]
* layout-reorders-reduction
* ndiff/total: 4/327680
* of those, real value differences: 4; NaN-payload-only: 0
* elements still holding the input sentinel: reference 1, candidate 1 (that side never wrote them)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0098
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0098/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0098/output.ttgir
triton-opt artifacts/HIT-0098/input.ttgir --tritongpu-remove-layout-conversions -o /tmp/HIT-0098.ttgir
diff artifacts/HIT-0098/output.ttgir /tmp/HIT-0098.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **1007**  (`python -m fz.progen 1007` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_red_fused_amax_gt_where_1`, heuristic `reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": false,
 "max_autotune": false,
 "persistent_reductions": false,
 "cooperative_reductions": null,
 "multi_kernel": 1,
 "combo_kernels": null,
 "prefer_nd_tiling": null,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": false,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0098/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 7974433
import torch
import torch.nn.functional as F

SEED = 7974433
KNOBS = {'dynamic': False, 'backward': False, 'max_autotune': False, 'persistent_reductions': False, 'cooperative_reductions': None, 'multi_kernel': 1, 'combo_kernels': None, 'prefer_nd_tiling': None, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': False, 'benchmark_kernel': False}
BACKWARD = False


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(7974433)
    a0 = torch.randn((61, 512), device=device, dtype=torch.float32, generator=g).to(torch.float16)
    return [a0]


def prog(a0):
    v1 = torch.where(a0 > a0, a0, a0)
    v2 = (v1.float() - v1.float().mean(-1, keepdim=True))
    v3 = v2.to(torch.int32)
    v4 = torch.amax(v1, dim=0, keepdim=False)
    return (v3, v4)

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0098/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 1007
# source file  : cta6gh7vye4tevophtramv7nik2bifxg462wlb3hqf377sofngvm.py
# inductor name: triton_red_fused_amax_gt_where_1
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_red_fused_amax_gt_where_1_2f917fc6(in_ptr0, out_ptr0, xnumel, r0_numel, XBLOCK : tl.constexpr, R0_BLOCK : tl.constexpr):
    xnumel = 64
    r0_numel = 7
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = xindex < xnumel
    r0_base = tl.arange(0, R0_BLOCK)[None, :]
    rbase = r0_base
    x0 = xindex
    _tmp4 = tl.full([XBLOCK, R0_BLOCK], float("-inf"), tl.float32)
    for r0_offset in tl.range(0, r0_numel, R0_BLOCK):
        r0_index = r0_offset + r0_base
        r0_mask = r0_index < r0_numel
        roffset = r0_offset
        rindex = r0_index
        r0_1 = r0_index
        tmp0 = tl.load(in_ptr0 + (x0 + 512*r0_1), r0_mask & xmask, eviction_policy='evict_first', other=0.0).to(tl.float32)
        tmp1 = tmp0 > tmp0
        tmp2 = tl.where(tmp1, tmp0, tmp0)
        tmp3 = tl.broadcast_to(tmp2, [XBLOCK, R0_BLOCK])
        tmp5 = triton_helpers.maximum(_tmp4, tmp3)
        _tmp4 = tl.where(r0_mask & xmask, tmp5, _tmp4)
    tmp4 = triton_helpers.max2(_tmp4, 1)[:, None]
    tl.store(out_ptr0 + (x0), tmp4, xmask)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(1, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "xnumel": 64,
 "r0_numel": 7
}
```

Base-pointer alignment class `elem`, element offsets per pointer argument:
`[0, 4]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 2,
 "num_ctas": 1,
 "num_stages": 8,
 "maxnreg": null,
 "default_dot_input_precision": "tf32",
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": true,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp16",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 64
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 7
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 64
 },
 {
  "name": "R0_BLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 8
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
--tritongpu-coalesce
```
candidate (same, plus the culprit):
```
--tritongpu-coalesce \
      --tritongpu-remove-layout-conversions
```

## IR

* `artifacts/HIT-0098/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0098/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0098/output.ttgir` -- after the reference pipeline + `--tritongpu-remove-layout-conversions`
* `artifacts/HIT-0098/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [4, 8], warpsPerCTA = [2, 1], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [8, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 2], order = [0, 1]}>
@@ -3,4 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [2], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [2, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 2], order = [0, 1]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [8, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 2], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [2, 1], order = [0, 1]}>
@@ -9,5 +6,2 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<64x8xf16, #blocked>
-    %cst_0 = arith.constant dense<512> : tensor<1x8xi32, #blocked>
-    %cst_1 = arith.constant dense<7> : tensor<1x8xi32, #blocked>
-    %cst_2 = arith.constant dense<0xFF800000> : tensor<64x8xf32, #blocked>
-    %cst_3 = arith.constant dense<64> : tensor<64x1xi32, #blocked1>
+    %cst = arith.constant dense<0xFF800000> : tensor<64x8xf32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<64x8xf16, #blocked>
@@ -14,0 +9,4 @@
+    %cst_1 = arith.constant dense<64> : tensor<64x1xi32, #blocked1>
+    %cst_2 = arith.constant dense<64> : tensor<64x1xi32, #blocked>
+    %cst_3 = arith.constant dense<512> : tensor<1x8xi32, #blocked>
+    %cst_4 = arith.constant dense<7> : tensor<1x8xi32, #blocked>
@@ -17,40 +15,33 @@
-    %2 = tt.make_range {end = 64 : i32, start = 0 : i32} : tensor<64xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<64xi32, #blocked2> -> tensor<64xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<64xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<64x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<64x1xi32, #blocked3> -> tensor<64x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<64x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<64x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_3 : tensor<64x1xi32, #blocked1>
-    %9 = tt.make_range {end = 8 : i32, start = 0 : i32} : tensor<8xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<8xi32, #blocked2> -> tensor<8xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<8xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x8xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x8xi32, #blocked4> -> tensor<1x8xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x8xi32, #blocked4> -> tensor<1x8xi32, #blocked>
-    %14 = arith.cmpi slt, %13, %cst_1 : tensor<1x8xi32, #blocked>
-    %15 = arith.muli %12, %cst_0 : tensor<1x8xi32, #blocked>
-    %16 = tt.broadcast %7 : tensor<64x1xi32, #blocked1> -> tensor<64x8xi32, #blocked1>
-    %17 = ttg.convert_layout %16 : tensor<64x8xi32, #blocked1> -> tensor<64x8xi32, #blocked>
-    %18 = tt.broadcast %15 : tensor<1x8xi32, #blocked> -> tensor<64x8xi32, #blocked>
-    %19 = arith.addi %17, %18 : tensor<64x8xi32, #blocked>
-    %20 = tt.splat %arg0 : !tt.ptr<f16> -> tensor<64x8x!tt.ptr<f16>, #blocked>
-    %21 = tt.addptr %20, %19 : tensor<64x8x!tt.ptr<f16>, #blocked>, tensor<64x8xi32, #blocked>
-    %22 = tt.broadcast %14 : tensor<1x8xi1, #blocked> -> tensor<64x8xi1, #blocked>
-    %23 = tt.broadcast %8 : tensor<64x1xi1, #blocked1> -> tensor<64x8xi1, #blocked1>
-    %24 = ttg.convert_layout %23 : tensor<64x8xi1, #blocked1> -> tensor<64x8xi1, #blocked>
-    %25 = arith.andi %22, %24 : tensor<64x8xi1, #blocked>
-    %26 = ttg.convert_layout %21 : tensor<64x8x!tt.ptr<f16>, #blocked> -> tensor<64x8x!tt.ptr<f16>, #blocked5>
-    %27 = ttg.convert_layout %25 : tensor<64x8xi1, #blocked> -> tensor<64x8xi1, #blocked5>
-    %28 = ttg.convert_layout %cst : tensor<64x8xf16, #blocked> -> tensor<64x8xf16, #blocked5>
-    %29 = tt.load %26, %27, %28 evictionPolicy = evict_first : tensor<64x8x!tt.ptr<f16>, #blocked5>
-    %30 = ttg.convert_layout %29 : tensor<64x8xf16, #blocked5> -> tensor<64x8xf16, #blocked>
-    %31 = arith.extf %30 : tensor<64x8xf16, #blocked> to tensor<64x8xf32, #blocked>
-    %32 = arith.cmpf ogt, %cst_2, %31 : tensor<64x8xf32, #blocked>
-    %33 = arith.cmpf une, %cst_2, %cst_2 : tensor<64x8xf32, #blocked>
-    %34 = arith.ori %32, %33 : tensor<64x8xi1, #blocked>
-    %35 = arith.select %34, %cst_2, %31 : tensor<64x8xi1, #blocked>, tensor<64x8xf32, #blocked>
-    %36 = tt.broadcast %14 : tensor<1x8xi1, #blocked> -> tensor<64x8xi1, #blocked>
-    %37 = tt.broadcast %8 : tensor<64x1xi1, #blocked1> -> tensor<64x8xi1, #blocked1>
-    %38 = ttg.convert_layout %37 : tensor<64x8xi1, #blocked1> -> tensor<64x8xi1, #blocked>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 14**: fill mode `signedzero`, fill seed `24499543`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `983055` (slot `1`, i.e. pointer
argument index `1`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 14,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "signedzero",
 "secs": 0.123,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 1,
   "dtype": "torch.float16",
   "n": 163840,
   "ndiff": 4,
   "ndiff_ref_unwritten": 1,
   "ndiff_cand_unwritten": 1,
   "first_index": 7,
   "max_abs_diff": 0.0,
   "max_abs_value": 4.76953125,
   "ref_value": -0.0,
   "cand_value": 0.0,
   "ref_bits": "0x8000",
   "cand_bits": "0x0",
   "max_ulp": 0,
   "ndiff_nan_payload": 0,
   "ndiff_value": 4,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0,
   "first_value_index": 7
  }
 ],
 "ndiff_total": 4,
 "n_total": 327680,
 "ndiff_value_total": 4,
 "ndiff_nan_payload_total": 0,
 "ndiff_ref_unwritten": 1,
 "ndiff_cand_unwritten": 1,
 "max_abs_diff": 0.0,
 "max_abs_value": 4.76953125,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addi`, `arith.andi`, `arith.cmpf`, `arith.cmpi`, `arith.constant`, `arith.extf`, `arith.muli`, `arith.ori`, `arith.select`, `arith.truncf`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.load`, `tt.make_range`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`, `ttg.convert_layout`, `ttg.slice`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: `blocked3`, `blocked4`, `blocked5`

**Diff size**: +54 / -66 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(1, 1, 1)` (programs = 1). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -1 +1 @@
-#blocked = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [4, 8], warpsPerCTA = [2, 1], order = [1, 0]}>
+#blocked = #ttg.blocked<{sizePerThread = [8, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 2], order = [0, 1]}>
@@ -3,4 +3 @@
-#blocked2 = #ttg.blocked<{sizePerThread = [1], threadsPerWarp = [32], warpsPerCTA = [2], order = [0]}>
-#blocked3 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [2, 1], order = [0, 1]}>
-#blocked4 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [1, 32], warpsPerCTA = [1, 2], order = [0, 1]}>
-#blocked5 = #ttg.blocked<{sizePerThread = [8, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 2], order = [0, 1]}>
+#blocked2 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [32, 1], warpsPerCTA = [2, 1], order = [0, 1]}>
@@ -9,5 +6,2 @@
-    %cst = arith.constant dense<0.000000e+00> : tensor<64x8xf16, #blocked>
-    %cst_0 = arith.constant dense<512> : tensor<1x8xi32, #blocked>
-    %cst_1 = arith.constant dense<7> : tensor<1x8xi32, #blocked>
-    %cst_2 = arith.constant dense<0xFF800000> : tensor<64x8xf32, #blocked>
-    %cst_3 = arith.constant dense<64> : tensor<64x1xi32, #blocked1>
+    %cst = arith.constant dense<0xFF800000> : tensor<64x8xf32, #blocked>
+    %cst_0 = arith.constant dense<0.000000e+00> : tensor<64x8xf16, #blocked>
@@ -14,0 +9,4 @@
+    %cst_1 = arith.constant dense<64> : tensor<64x1xi32, #blocked1>
+    %cst_2 = arith.constant dense<64> : tensor<64x1xi32, #blocked>
+    %cst_3 = arith.constant dense<512> : tensor<1x8xi32, #blocked>
+    %cst_4 = arith.constant dense<7> : tensor<1x8xi32, #blocked>
@@ -17,40 +15,33 @@
-    %2 = tt.make_range {end = 64 : i32, start = 0 : i32} : tensor<64xi32, #blocked2>
-    %3 = ttg.convert_layout %2 : tensor<64xi32, #blocked2> -> tensor<64xi32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %4 = tt.expand_dims %3 {axis = 1 : i32} : tensor<64xi32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<64x1xi32, #blocked3>
-    %5 = ttg.convert_layout %4 : tensor<64x1xi32, #blocked3> -> tensor<64x1xi32, #blocked1>
-    %6 = tt.splat %1 : i32 -> tensor<64x1xi32, #blocked1>
-    %7 = arith.addi %6, %5 : tensor<64x1xi32, #blocked1>
-    %8 = arith.cmpi slt, %7, %cst_3 : tensor<64x1xi32, #blocked1>
-    %9 = tt.make_range {end = 8 : i32, start = 0 : i32} : tensor<8xi32, #blocked2>
-    %10 = ttg.convert_layout %9 : tensor<8xi32, #blocked2> -> tensor<8xi32, #ttg.slice<{dim = 0, parent = #blocked4}>>
-    %11 = tt.expand_dims %10 {axis = 0 : i32} : tensor<8xi32, #ttg.slice<{dim = 0, parent = #blocked4}>> -> tensor<1x8xi32, #blocked4>
-    %12 = ttg.convert_layout %11 : tensor<1x8xi32, #blocked4> -> tensor<1x8xi32, #blocked>
-    %13 = ttg.convert_layout %11 : tensor<1x8xi32, #blocked4> -> tensor<1x8xi32, #blocked>
-    %14 = arith.cmpi slt, %13, %cst_1 : tensor<1x8xi32, #blocked>
-    %15 = arith.muli %12, %cst_0 : tensor<1x8xi32, #blocked>
-    %16 = tt.broadcast %7 : tensor<64x1xi32, #blocked1> -> tensor<64x8xi32, #blocked1>
-    %17 = ttg.convert_layout %16 : tensor<64x8xi32, #blocked1> -> tensor<64x8xi32, #blocked>
-    %18 = tt.broadcast %15 : tensor<1x8xi32, #blocked> -> tensor<64x8xi32, #blocked>
-    %19 = arith.addi %17, %18 : tensor<64x8xi32, #blocked>
-    %20 = tt.splat %arg0 : !tt.ptr<f16> -> tensor<64x8x!tt.ptr<f16>, #blocked>
-    %21 = tt.addptr %20, %19 : tensor<64x8x!tt.ptr<f16>, #blocked>, tensor<64x8xi32, #blocked>
-    %22 = tt.broadcast %14 : tensor<1x8xi1, #blocked> -> tensor<64x8xi1, #blocked>
-    %23 = tt.broadcast %8 : tensor<64x1xi1, #blocked1> -> tensor<64x8xi1, #blocked1>
-    %24 = ttg.convert_layout %23 : tensor<64x8xi1, #blocked1> -> tensor<64x8xi1, #blocked>
-    %25 = arith.andi %22, %24 : tensor<64x8xi1, #blocked>
-    %26 = ttg.convert_layout %21 : tensor<64x8x!tt.ptr<f16>, #blocked> -> tensor<64x8x!tt.ptr<f16>, #blocked5>
-    %27 = ttg.convert_layout %25 : tensor<64x8xi1, #blocked> -> tensor<64x8xi1, #blocked5>
-    %28 = ttg.convert_layout %cst : tensor<64x8xf16, #blocked> -> tensor<64x8xf16, #blocked5>
-    %29 = tt.load %26, %27, %28 evictionPolicy = evict_first : tensor<64x8x!tt.ptr<f16>, #blocked5>
-    %30 = ttg.convert_layout %29 : tensor<64x8xf16, #blocked5> -> tensor<64x8xf16, #blocked>
-    %31 = arith.extf %30 : tensor<64x8xf16, #blocked> to tensor<64x8xf32, #blocked>
-    %32 = arith.cmpf ogt, %cst_2, %31 : tensor<64x8xf32, #blocked>
-    %33 = arith.cmpf une, %cst_2, %cst_2 : tensor<64x8xf32, #blocked>
-    %34 = arith.ori %32, %33 : tensor<64x8xi1, #blocked>
-    %35 = arith.select %34, %cst_2, %31 : tensor<64x8xi1, #blocked>, tensor<64x8xf32, #blocked>
-    %36 = tt.broadcast %14 : tensor<1x8xi1, #blocked> -> tensor<64x8xi1, #blocked>
-    %37 = tt.broadcast %8 : tensor<64x1xi1, #blocked1> -> tensor<64x8xi1, #blocked1>
-    %38 = ttg.convert_layout %37 : tensor<64x8xi1, #blocked1> -> tensor<64x8xi1, #blocked>
```

