# HIT-0149: tritongpu-coalesce on gen:triton_per_fused_amin_neg_sin_0_de0174a7
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--tritongpu-coalesce`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=ieee, enable_fp_fusion=False, launch_cooperative_grid=False, launch_pdl=False, maxnreg=None, num_ctas=1, num_stages=3, num_warps=1, sanitize_overflow=True
* base-pointer alignment: aligned (element offsets [0, 0, 0])
* shape draw: numels={'xnumel': 67, 'r0_numel': 1} blocks={'XBLOCK': 32} grid=[3, 1, 1]
* layout-reorders-reduction
* ndiff/total: 67/98304
* of those, real value differences: 0; NaN-payload-only: 67
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0149
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0149/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0149/output.ttgir
triton-opt artifacts/HIT-0149/input.ttgir --tritongpu-coalesce -o /tmp/HIT-0149.ttgir
diff artifacts/HIT-0149/output.ttgir /tmp/HIT-0149.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **3148**  (`python -m fz.progen 3148` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_per_fused_amin_neg_sin_0`, heuristic `persistent_reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": false,
 "max_autotune": false,
 "persistent_reductions": true,
 "cooperative_reductions": null,
 "multi_kernel": null,
 "combo_kernels": null,
 "prefer_nd_tiling": true,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": null,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0149/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 24929012
import torch
import torch.nn.functional as F

SEED = 24929012
KNOBS = {'dynamic': False, 'backward': False, 'max_autotune': False, 'persistent_reductions': True, 'cooperative_reductions': None, 'multi_kernel': None, 'combo_kernels': None, 'prefer_nd_tiling': True, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': None, 'benchmark_kernel': False}
BACKWARD = False


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(24929012)
    a0 = torch.randn((17, 13), device=device, dtype=torch.float32, generator=g).to(torch.bfloat16)
    a1 = torch.randn((17, 13), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    c0 = torch.randn((13, 37), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    return [a0, a1, c0]


def prog(a0, a1, c0):
    v1 = torch.matmul(a1, c0)
    v2 = torch.sin(v1)
    v3 = F.gelu(c0)
    v4 = torch.sin(v2)
    v5 = torch.sort(v4, dim=1)[0]
    v6 = torch.where(v2 > v1, v2, v1)
    v7 = torch.amax(v5, dim=0, keepdim=False)
    v8 = v5 * 0.5415
    v9 = v5.to(torch.int32)
    v10 = torch.amin(v2, dim=0, keepdim=False)
    v11 = -v4
    v12 = torch.sum(v10, dim=0, keepdim=False)
    return (v11, v12)

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0149/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 3148
# source file  : cyvaykt5d4hyz4s5h5lpegw7popsvbw23nvkxib4gmzotzqrhdhc.py
# inductor name: triton_per_fused_amin_neg_sin_0
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_per_fused_amin_neg_sin_0_de0174a7(in_ptr0, out_ptr0, out_ptr1, xnumel, r0_numel, XBLOCK : tl.constexpr):
    xnumel = 67
    r0_numel = 1
    R0_BLOCK: tl.constexpr = 32
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = xindex < xnumel
    r0_index = tl.arange(0, R0_BLOCK)[None, :]
    r0_offset = 0
    r0_mask = r0_index < r0_numel
    roffset = r0_offset
    rindex = r0_index
    r0_1 = r0_index
    x0 = xindex
    tmp0 = tl.load(in_ptr0 + (x0 + 37*r0_1), r0_mask & xmask, eviction_policy='evict_first', other=0.0)
    tmp1 = tl_math.sin(tmp0)
    tmp2 = tl_math.sin(tmp1)
    tmp3 = -tmp2
    tmp4 = tl.broadcast_to(tmp1, [XBLOCK, R0_BLOCK])
    tmp6 = tl.where(r0_mask & xmask, tmp4, float("inf"))
    tmp7 = triton_helpers.min2(tmp6, 1)[:, None].to(tl.float32)
    tl.store(out_ptr0 + (x0 + 37*r0_1), tmp3, r0_mask & xmask)
    tl.store(out_ptr1 + (x0), tmp7, xmask)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(3, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "xnumel": 67,
 "r0_numel": 1
}
```

Base-pointer alignment class `aligned`, element offsets per pointer argument:
`[0, 0, 0]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 1,
 "num_ctas": 1,
 "num_stages": 3,
 "maxnreg": null,
 "default_dot_input_precision": "ieee",
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": true,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr1",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 67
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 1
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 32
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--tritongpu-coalesce
```

## IR

* `artifacts/HIT-0149/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0149/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0149/output.ttgir` -- after the reference pipeline + `--tritongpu-coalesce`
* `artifacts/HIT-0149/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -41,10 +41,14 @@
-    %27 = tt.load %22, %26, %cst_0 evictionPolicy = evict_first : tensor<32x32x!tt.ptr<f32>, #blocked>
-    %28 = math.sin %27 : tensor<32x32xf32, #blocked>
-    %29 = math.sin %28 : tensor<32x32xf32, #blocked>
-    %30 = arith.negf %29 : tensor<32x32xf32, #blocked>
-    %31 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
-    %32 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
-    %33 = ttg.convert_layout %32 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
-    %34 = arith.andi %31, %33 : tensor<32x32xi1, #blocked>
-    %35 = arith.select %34, %28, %cst : tensor<32x32xi1, #blocked>, tensor<32x32xf32, #blocked>
-    %36 = "tt.reduce"(%35) <{axis = 1 : i32}> ({
+    %27 = ttg.convert_layout %22 : tensor<32x32x!tt.ptr<f32>, #blocked> -> tensor<32x32x!tt.ptr<f32>, #blocked3>
+    %28 = ttg.convert_layout %26 : tensor<32x32xi1, #blocked> -> tensor<32x32xi1, #blocked3>
+    %29 = ttg.convert_layout %cst_0 : tensor<32x32xf32, #blocked> -> tensor<32x32xf32, #blocked3>
+    %30 = tt.load %27, %28, %29 evictionPolicy = evict_first : tensor<32x32x!tt.ptr<f32>, #blocked3>
+    %31 = ttg.convert_layout %30 : tensor<32x32xf32, #blocked3> -> tensor<32x32xf32, #blocked>
+    %32 = math.sin %31 : tensor<32x32xf32, #blocked>
+    %33 = math.sin %32 : tensor<32x32xf32, #blocked>
+    %34 = arith.negf %33 : tensor<32x32xf32, #blocked>
+    %35 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
+    %36 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
+    %37 = ttg.convert_layout %36 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
+    %38 = arith.andi %35, %37 : tensor<32x32xi1, #blocked>
+    %39 = arith.select %38, %32, %cst : tensor<32x32xi1, #blocked>, tensor<32x32xf32, #blocked>
+    %40 = "tt.reduce"(%39) <{axis = 1 : i32}> ({
@@ -52,5 +56,5 @@
-      %54 = arith.cmpf olt, %arg5, %arg6 : f32
-      %55 = arith.cmpf une, %arg5, %arg5 : f32
-      %56 = arith.ori %54, %55 : i1
-      %57 = arith.select %56, %arg5, %arg6 : f32
-      tt.reduce.return %57 : f32
+      %64 = arith.cmpf olt, %arg5, %arg6 : f32
+      %65 = arith.cmpf une, %arg5, %arg5 : f32
+      %66 = arith.ori %64, %65 : i1
+      %67 = arith.select %66, %arg5, %arg6 : f32
+      tt.reduce.return %67 : f32
@@ -58,19 +62,25 @@
-    %37 = ttg.convert_layout %36 : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked}>> -> tensor<32xf32, #blocked2>
-    %38 = ttg.convert_layout %37 : tensor<32xf32, #blocked2> -> tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %39 = tt.expand_dims %38 {axis = 1 : i32} : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<32x1xf32, #blocked3>
-    %40 = ttg.convert_layout %39 : tensor<32x1xf32, #blocked3> -> tensor<32x1xf32, #blocked1>
-    %41 = arith.muli %12, %cst_1 : tensor<1x32xi32, #blocked>
-    %42 = tt.broadcast %7 : tensor<32x1xi32, #blocked1> -> tensor<32x32xi32, #blocked1>
-    %43 = ttg.convert_layout %42 : tensor<32x32xi32, #blocked1> -> tensor<32x32xi32, #blocked>
-    %44 = tt.broadcast %41 : tensor<1x32xi32, #blocked> -> tensor<32x32xi32, #blocked>
-    %45 = arith.addi %43, %44 : tensor<32x32xi32, #blocked>
-    %46 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<32x32x!tt.ptr<f32>, #blocked>
-    %47 = tt.addptr %46, %45 : tensor<32x32x!tt.ptr<f32>, #blocked>, tensor<32x32xi32, #blocked>
-    %48 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
-    %49 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
-    %50 = ttg.convert_layout %49 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
-    %51 = arith.andi %48, %50 : tensor<32x32xi1, #blocked>
-    tt.store %47, %30, %51 : tensor<32x32x!tt.ptr<f32>, #blocked>
-    %52 = tt.splat %arg2 : !tt.ptr<f32> -> tensor<32x1x!tt.ptr<f32>, #blocked1>
-    %53 = tt.addptr %52, %7 : tensor<32x1x!tt.ptr<f32>, #blocked1>, tensor<32x1xi32, #blocked1>
-    tt.store %53, %40, %8 : tensor<32x1x!tt.ptr<f32>, #blocked1>
+    %41 = ttg.convert_layout %40 : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked}>> -> tensor<32xf32, #blocked2>
+    %42 = ttg.convert_layout %41 : tensor<32xf32, #blocked2> -> tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>>
+    %43 = tt.expand_dims %42 {axis = 1 : i32} : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<32x1xf32, #blocked3>
+    %44 = ttg.convert_layout %43 : tensor<32x1xf32, #blocked3> -> tensor<32x1xf32, #blocked1>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 2**: fill mode `nanpayload`, fill seed `24367352`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `393219` (slot `1`, i.e. pointer
argument index `1`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 2,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "nanpayload",
 "secs": 0.054,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 1,
   "dtype": "torch.float32",
   "n": 32768,
   "ndiff": 67,
   "ndiff_ref_unwritten": 0,
   "ndiff_cand_unwritten": 0,
   "first_index": 0,
   "max_abs_diff": 0.0,
   "max_abs_value": 0.0,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0xffffffff",
   "cand_bits": "0x7fffffff",
   "max_ulp": 0,
   "ndiff_nan_payload": 67,
   "ndiff_value": 0,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0
  }
 ],
 "ndiff_total": 67,
 "n_total": 98304,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 67,
 "ndiff_ref_unwritten": 0,
 "ndiff_cand_unwritten": 0,
 "max_abs_diff": 0.0,
 "max_abs_value": 0.0,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)
* all ops touched by the diff: `arith.addi`, `arith.andi`, `arith.cmpf`, `arith.muli`, `arith.negf`, `arith.ori`, `arith.select`, `math.sin`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.load`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.convert_layout`, `ttg.slice`

**Layout attributes in the IR diff**

* only in the output IR: (none)
* only in the input IR: (none)

**Diff size**: +44 / -34 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(3, 1, 1)` (programs = 3). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -41,10 +41,14 @@
-    %27 = tt.load %22, %26, %cst_0 evictionPolicy = evict_first : tensor<32x32x!tt.ptr<f32>, #blocked>
-    %28 = math.sin %27 : tensor<32x32xf32, #blocked>
-    %29 = math.sin %28 : tensor<32x32xf32, #blocked>
-    %30 = arith.negf %29 : tensor<32x32xf32, #blocked>
-    %31 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
-    %32 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
-    %33 = ttg.convert_layout %32 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
-    %34 = arith.andi %31, %33 : tensor<32x32xi1, #blocked>
-    %35 = arith.select %34, %28, %cst : tensor<32x32xi1, #blocked>, tensor<32x32xf32, #blocked>
-    %36 = "tt.reduce"(%35) <{axis = 1 : i32}> ({
+    %27 = ttg.convert_layout %22 : tensor<32x32x!tt.ptr<f32>, #blocked> -> tensor<32x32x!tt.ptr<f32>, #blocked3>
+    %28 = ttg.convert_layout %26 : tensor<32x32xi1, #blocked> -> tensor<32x32xi1, #blocked3>
+    %29 = ttg.convert_layout %cst_0 : tensor<32x32xf32, #blocked> -> tensor<32x32xf32, #blocked3>
+    %30 = tt.load %27, %28, %29 evictionPolicy = evict_first : tensor<32x32x!tt.ptr<f32>, #blocked3>
+    %31 = ttg.convert_layout %30 : tensor<32x32xf32, #blocked3> -> tensor<32x32xf32, #blocked>
+    %32 = math.sin %31 : tensor<32x32xf32, #blocked>
+    %33 = math.sin %32 : tensor<32x32xf32, #blocked>
+    %34 = arith.negf %33 : tensor<32x32xf32, #blocked>
+    %35 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
+    %36 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
+    %37 = ttg.convert_layout %36 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
+    %38 = arith.andi %35, %37 : tensor<32x32xi1, #blocked>
+    %39 = arith.select %38, %32, %cst : tensor<32x32xi1, #blocked>, tensor<32x32xf32, #blocked>
+    %40 = "tt.reduce"(%39) <{axis = 1 : i32}> ({
@@ -52,5 +56,5 @@
-      %54 = arith.cmpf olt, %arg5, %arg6 : f32
-      %55 = arith.cmpf une, %arg5, %arg5 : f32
-      %56 = arith.ori %54, %55 : i1
-      %57 = arith.select %56, %arg5, %arg6 : f32
-      tt.reduce.return %57 : f32
+      %64 = arith.cmpf olt, %arg5, %arg6 : f32
+      %65 = arith.cmpf une, %arg5, %arg5 : f32
+      %66 = arith.ori %64, %65 : i1
+      %67 = arith.select %66, %arg5, %arg6 : f32
+      tt.reduce.return %67 : f32
@@ -58,19 +62,25 @@
-    %37 = ttg.convert_layout %36 : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked}>> -> tensor<32xf32, #blocked2>
-    %38 = ttg.convert_layout %37 : tensor<32xf32, #blocked2> -> tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>>
-    %39 = tt.expand_dims %38 {axis = 1 : i32} : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<32x1xf32, #blocked3>
-    %40 = ttg.convert_layout %39 : tensor<32x1xf32, #blocked3> -> tensor<32x1xf32, #blocked1>
-    %41 = arith.muli %12, %cst_1 : tensor<1x32xi32, #blocked>
-    %42 = tt.broadcast %7 : tensor<32x1xi32, #blocked1> -> tensor<32x32xi32, #blocked1>
-    %43 = ttg.convert_layout %42 : tensor<32x32xi32, #blocked1> -> tensor<32x32xi32, #blocked>
-    %44 = tt.broadcast %41 : tensor<1x32xi32, #blocked> -> tensor<32x32xi32, #blocked>
-    %45 = arith.addi %43, %44 : tensor<32x32xi32, #blocked>
-    %46 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<32x32x!tt.ptr<f32>, #blocked>
-    %47 = tt.addptr %46, %45 : tensor<32x32x!tt.ptr<f32>, #blocked>, tensor<32x32xi32, #blocked>
-    %48 = tt.broadcast %15 : tensor<1x32xi1, #blocked> -> tensor<32x32xi1, #blocked>
-    %49 = tt.broadcast %8 : tensor<32x1xi1, #blocked1> -> tensor<32x32xi1, #blocked1>
-    %50 = ttg.convert_layout %49 : tensor<32x32xi1, #blocked1> -> tensor<32x32xi1, #blocked>
-    %51 = arith.andi %48, %50 : tensor<32x32xi1, #blocked>
-    tt.store %47, %30, %51 : tensor<32x32x!tt.ptr<f32>, #blocked>
-    %52 = tt.splat %arg2 : !tt.ptr<f32> -> tensor<32x1x!tt.ptr<f32>, #blocked1>
-    %53 = tt.addptr %52, %7 : tensor<32x1x!tt.ptr<f32>, #blocked1>, tensor<32x1xi32, #blocked1>
-    tt.store %53, %40, %8 : tensor<32x1x!tt.ptr<f32>, #blocked1>
+    %41 = ttg.convert_layout %40 : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked}>> -> tensor<32xf32, #blocked2>
+    %42 = ttg.convert_layout %41 : tensor<32xf32, #blocked2> -> tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>>
+    %43 = tt.expand_dims %42 {axis = 1 : i32} : tensor<32xf32, #ttg.slice<{dim = 1, parent = #blocked3}>> -> tensor<32x1xf32, #blocked3>
+    %44 = ttg.convert_layout %43 : tensor<32x1xf32, #blocked3> -> tensor<32x1xf32, #blocked1>
```

