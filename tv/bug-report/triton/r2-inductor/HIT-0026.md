# HIT-0026: tritongpu-coalesce on gen:triton_red_fused_abs_add_amax_fmod_neg_tanh_1_05dfb1e9
Verdict: CONFIRMED
Arch:    generic
Line:    r2-inductor
Culprit pass: `--tritongpu-coalesce`  (class: layout)
SMT verdict: TODO (reviewing agent)

Summary: this line generates its own kernels -- a random torch program is
compiled by TorchInductor and the Triton kernel it emits becomes the corpus
entry. Compiling that kernel with the pipeline below, and with the same pipeline
plus one extra pass, produces cubins whose outputs differ bit for bit on the
same input. That is the whole claim: a bit difference between two compilations.
No judgement about whether it is a compiler bug is made here -- the labels below
carry the material a reviewer needs. The kernel does contain a tt.dot / tt.reduce.

## Labels

* pass-class: layout
* compile config: default_dot_input_precision=ieee, enable_fp_fusion=False, launch_cooperative_grid=False, launch_pdl=False, maxnreg=None, num_ctas=1, num_stages=7, num_warps=32, sanitize_overflow=False
* base-pointer alignment: aligned (element offsets [0, 0, 0, 0, 0])
* shape draw: numels={'xnumel': 101, 'r0_numel': 71} blocks={'XBLOCK': 8, 'R0_BLOCK': 256} grid=[13, 1, 1]
* layout-reorders-reduction
* ndiff/total: 7171/7569920
* of those, real value differences: 0; NaN-payload-only: 7171
* NaN-PAYLOAD-ONLY difference (no computed value differs anywhere)
* max_abs_diff: 0.0
* max_ulp: 0

Controls:

* heap shift: survives a 28 KiB shift of every buffer
* small whole-number inputs: does not differ with small whole-number inputs (consistent with, but not proof of, reassociation)

## Reproduction manual

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
python -m fz.repro --hit HIT-0026
```

That script rebuilds both sides from the saved spec and reruns the comparison.
To do it by hand:

```bash
source /home/youngzt/fuzz/r2-inductor/env.sh
cd /home/youngzt/fuzz/r2-inductor
# reference IR (already saved):  artifacts/HIT-0026/input.ttgir
# candidate IR (already saved):  artifacts/HIT-0026/output.ttgir
triton-opt artifacts/HIT-0026/input.ttgir --tritongpu-coalesce -o /tmp/HIT-0026.ttgir
diff artifacts/HIT-0026/output.ttgir /tmp/HIT-0026.ttgir     # must be empty
```

Expected: the two cubins give bit-identical output buffers.
Actual: they differ (see Observed).

## Where the kernel came from

This kernel does not exist in any checked-in corpus; it was generated during the
run. Everything needed to rebuild it is here.

* generator seed: **391**  (`python -m fz.progen 391` prints the
  torch program; the run used `fz.harvest_run` to compile it)
* inductor kernel name: `triton_red_fused_abs_add_amax_fmod_neg_tanh_1`, heuristic `reduction`
* inductor config knobs used for that compile:

```json
{
 "dynamic": false,
 "backward": false,
 "max_autotune": false,
 "persistent_reductions": null,
 "cooperative_reductions": null,
 "multi_kernel": null,
 "combo_kernels": null,
 "prefer_nd_tiling": null,
 "unroll_reductions_threshold": null,
 "assume_aligned_inputs": null,
 "benchmark_kernel": false
}
```

### The torch program (artifacts/HIT-0026/program.py)

```python
# Auto-generated torch program, r2-inductor fuzzing line.
# seed = 3096329
import torch
import torch.nn.functional as F

SEED = 3096329
KNOBS = {'dynamic': False, 'backward': False, 'max_autotune': False, 'persistent_reductions': None, 'cooperative_reductions': None, 'multi_kernel': None, 'combo_kernels': None, 'prefer_nd_tiling': None, 'unroll_reductions_threshold': None, 'assume_aligned_inputs': None, 'benchmark_kernel': False}
BACKWARD = False


def make_inputs(device='cuda'):
    g = torch.Generator(device=device).manual_seed(3096329)
    a0 = torch.randn((101, 17, 256), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    a1 = torch.randn((101, 17, 256), device=device, dtype=torch.float32, generator=g).to(torch.float32)
    return [a0, a1]


def prog(a0, a1):
    v1 = torch.erf(a1)
    v2 = torch.maximum(v1, a0)
    v3 = torch.amax(v1, dim=2, keepdim=True)
    v4 = torch.trunc(a1)
    v5 = torch.sort(v4, dim=1)[0]
    v6 = torch.tanh(v5)
    v7 = (v6.float() * torch.rsqrt(v6.float().pow(2).mean(-1, keepdim=True) + 1e-6))
    v8 = torch.cat([v1, v2], dim=0)
    v9 = v4.transpose(1, 2)
    v10 = v7.flatten()
    v11 = -a1
    v12 = torch.fmod(v11, v6.abs() + 1.0)
    v13 = torch.amax(v6, dim=0, keepdim=True)
    return (v11, v12, v13)

```

### The generated Triton kernel, exactly as compiled (artifacts/HIT-0026/kernel.py)

The shape patch below was applied to the harvested source before compiling.

```python
# Harvested from TorchInductor by the r2-inductor fuzzing line.
# program seed : 391
# source file  : cjoixwsaersogz3sjzu4hcuumubcvsmthugek63fydspj77wcodo.py
# inductor name: triton_red_fused_abs_add_amax_fmod_neg_tanh_1
import triton
import triton.language as tl
from torch._inductor.runtime import triton_helpers
from torch._inductor.runtime.triton_helpers import libdevice, math as tl_math

math = tl_math


@triton.jit
def triton_red_fused_abs_add_amax_fmod_neg_tanh_1_05dfb1e9(in_ptr0, in_ptr1, out_ptr0, out_ptr1, out_ptr2, xnumel, r0_numel, XBLOCK : tl.constexpr, R0_BLOCK : tl.constexpr):
    xnumel = 101
    r0_numel = 71
    rnumel = r0_numel
    RBLOCK: tl.constexpr = R0_BLOCK
    xoffset = tl.program_id(0) * XBLOCK
    xindex = xoffset + tl.arange(0, XBLOCK)[:, None]
    xmask = xindex < xnumel
    r0_base = tl.arange(0, R0_BLOCK)[None, :]
    rbase = r0_base
    x0 = xindex
    _tmp9 = tl.full([XBLOCK, R0_BLOCK], float("-inf"), tl.float32)
    for r0_offset in tl.range(0, r0_numel, R0_BLOCK):
        r0_index = r0_offset + r0_base
        r0_mask = r0_index < r0_numel
        roffset = r0_offset
        rindex = r0_index
        r0_1 = r0_index
        tmp0 = tl.load(in_ptr0 + (x0 + 4352*r0_1), r0_mask & xmask, eviction_policy='evict_first', other=0.0)
        tmp2 = tl.load(in_ptr1 + (x0 + 4352*r0_1), r0_mask & xmask, eviction_policy='evict_first', other=0.0)
        tmp1 = -tmp0
        tmp3 = libdevice.tanh(tmp2)
        tmp4 = tl_math.abs(tmp3)
        tmp5 = tl.full([1, 1], 1.0, tl.float32)
        tmp6 = tmp4 + tmp5
        tmp7 = libdevice.fmod(tmp1, tmp6)
        tmp8 = tl.broadcast_to(tmp3, [XBLOCK, R0_BLOCK])
        tmp10 = triton_helpers.maximum(_tmp9, tmp8)
        _tmp9 = tl.where(r0_mask & xmask, tmp10, _tmp9)
        tl.store(out_ptr0 + (x0 + 4352*r0_1), tmp1, r0_mask & xmask)
        tl.store(out_ptr1 + (x0 + 4352*r0_1), tmp7, r0_mask & xmask)
    tmp9 = triton_helpers.max2(_tmp9, 1)[:, None]
    tl.store(out_ptr2 + (x0), tmp9, xmask)

```

## The draw: shape, alignment and compile config

Both arms of the comparison get this same draw; only the pass set differs.

Launch grid `(13, 1, 1)`. Shape patch applied to the kernel body:

```json
{
 "xnumel": 101,
 "r0_numel": 71
}
```

Base-pointer alignment class `aligned`, element offsets per pointer argument:
`[0, 0, 0, 0, 0]`. An offset that is not a multiple of 16 bytes means the harness
deliberately does **not** claim `tt.divisibility = 16` for that argument, so
`AxisInfo` sees the truth and the program stays well defined.

Compile config (PLAN-R2 section 2b):

```json
{
 "num_warps": 32,
 "num_ctas": 1,
 "num_stages": 7,
 "maxnreg": null,
 "default_dot_input_precision": "ieee",
 "launch_cooperative_grid": false,
 "launch_pdl": false,
 "sanitize_overflow": false,
 "enable_fp_fusion": false
}
```

Argument spec:

```json
[
 {
  "name": "in_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "in_ptr1",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr0",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr1",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "out_ptr2",
  "kind": "ptr",
  "ty": "*fp32",
  "value": null
 },
 {
  "name": "xnumel",
  "kind": "int",
  "ty": "i32",
  "value": 101
 },
 {
  "name": "r0_numel",
  "kind": "int",
  "ty": "i32",
  "value": 71
 },
 {
  "name": "XBLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 8
 },
 {
  "name": "R0_BLOCK",
  "kind": "constexpr",
  "ty": "constexpr",
  "value": 256
 }
]
```

## Pass pipeline: reference vs candidate

reference:
```
(none: the raw root TTGIR)
```
candidate (same, plus the culprit):
```
--tritongpu-coalesce
```

## IR

* `artifacts/HIT-0026/root.ttgir`   -- straight out of `convert-triton-to-tritongpu`
* `artifacts/HIT-0026/input.ttgir`  -- after the reference pipeline
* `artifacts/HIT-0026/output.ttgir` -- after the reference pipeline + `--tritongpu-coalesce`
* `artifacts/HIT-0026/facts.json`   -- mechanical facts about the diff

First 60 lines of the diff:

```mlir
@@ -5,0 +6,2 @@
+#blocked5 = #ttg.blocked<{sizePerThread = [2, 1], threadsPerWarp = [4, 8], warpsPerCTA = [1, 32], order = [0, 1]}>
+#blocked6 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 32], order = [0, 1]}>
@@ -44,52 +46,66 @@
-    %29 = tt.load %24, %28, %cst_0 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %30 = arith.muli %14, %cst_1 : tensor<1x256xi32, #blocked>
-    %31 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %32 = ttg.convert_layout %31 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %33 = tt.broadcast %30 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %34 = arith.addi %32, %33 : tensor<8x256xi32, #blocked>
-    %35 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %36 = tt.addptr %35, %34 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %37 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %38 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %39 = ttg.convert_layout %38 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %40 = arith.andi %37, %39 : tensor<8x256xi1, #blocked>
-    %41 = tt.load %36, %40, %cst_0 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %42 = arith.negf %29 : tensor<8x256xf32, #blocked>
-    %43 = tt.extern_elementwise %41 {libname = "", libpath = "", pure = true, symbol = "__nv_tanhf"} : (tensor<8x256xf32, #blocked>) -> tensor<8x256xf32, #blocked>
-    %44 = math.absf %43 : tensor<8x256xf32, #blocked>
-    %45 = arith.addf %44, %cst : tensor<8x256xf32, #blocked>
-    %46 = tt.extern_elementwise %42, %45 {libname = "", libpath = "", pure = true, symbol = "__nv_fmodf"} : (tensor<8x256xf32, #blocked>, tensor<8x256xf32, #blocked>) -> tensor<8x256xf32, #blocked>
-    %47 = arith.cmpf ogt, %cst_3, %43 : tensor<8x256xf32, #blocked>
-    %48 = arith.cmpf une, %cst_3, %cst_3 : tensor<8x256xf32, #blocked>
-    %49 = arith.ori %47, %48 : tensor<8x256xi1, #blocked>
-    %50 = arith.select %49, %cst_3, %43 : tensor<8x256xi1, #blocked>, tensor<8x256xf32, #blocked>
-    %51 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %52 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %53 = ttg.convert_layout %52 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %54 = arith.andi %51, %53 : tensor<8x256xi1, #blocked>
-    %55 = arith.select %54, %50, %cst_3 : tensor<8x256xi1, #blocked>, tensor<8x256xf32, #blocked>
-    %56 = arith.muli %13, %cst_1 : tensor<1x256xi32, #blocked>
-    %57 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %58 = ttg.convert_layout %57 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %59 = tt.broadcast %56 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %60 = arith.addi %58, %59 : tensor<8x256xi32, #blocked>
-    %61 = tt.splat %arg2 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %62 = tt.addptr %61, %60 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %63 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %64 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %65 = ttg.convert_layout %64 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %66 = arith.andi %63, %65 : tensor<8x256xi1, #blocked>
-    tt.store %62, %42, %66 : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %67 = arith.muli %12, %cst_1 : tensor<1x256xi32, #blocked>
-    %68 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %69 = ttg.convert_layout %68 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %70 = tt.broadcast %67 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %71 = arith.addi %69, %70 : tensor<8x256xi32, #blocked>
-    %72 = tt.splat %arg3 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %73 = tt.addptr %72, %71 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %74 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %75 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %76 = ttg.convert_layout %75 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %77 = arith.andi %74, %76 : tensor<8x256xi1, #blocked>
-    tt.store %73, %46, %77 : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %78 = "tt.reduce"(%55) <{axis = 1 : i32}> ({
+    %29 = ttg.convert_layout %24 : tensor<8x256x!tt.ptr<f32>, #blocked> -> tensor<8x256x!tt.ptr<f32>, #blocked5>
+    %30 = ttg.convert_layout %28 : tensor<8x256xi1, #blocked> -> tensor<8x256xi1, #blocked5>
+    %31 = ttg.convert_layout %cst_0 : tensor<8x256xf32, #blocked> -> tensor<8x256xf32, #blocked5>
+    %32 = tt.load %29, %30, %31 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked5>
```

## Inputs

Each compiled pair is run on 18 input draws. The one that differs is
**draw 5**: fill mode `nanpayload`, fill seed `8254634`. Every pointer argument
is a slot in one deterministically filled pool, so the mode plus the seed
reproduces the bytes exactly -- no tensor files are needed.

The draw index matters: every re-check below (self-consistency, pass bisection,
heap shift, the measurements, and `fz.repro`) replays **this** draw, not draw 0.

## Observed

First differing byte in the pool: `24223744` (slot `2`, i.e. pointer
argument index `2`).

## Determinism evidence

The same build run three times on the same input is bit-identical (pre-screen
gate 1, on the canonical shape). At **this** shape, each side was re-run under a
single binary and checked for self-consistency:

```json
{
 "ok": true,
 "reps": 3,
 "draw": 5,
 "ref_stable": true,
 "cand_stable": true,
 "mode": "nanpayload",
 "secs": 0.022,
 "id": null
}
```

The mismatch was then re-run 3 times on the differing draw, all three
reproduced, then re-run over the full sweep of 18 draws: verdict `"mismatch"`.

## Difference measurements

```json
{
 "slots": [
  {
   "slot": 2,
   "dtype": "torch.float32",
   "n": 1513984,
   "ndiff": 7171,
   "ndiff_ref_unwritten": 0,
   "ndiff_cand_unwritten": 0,
   "first_index": 0,
   "max_abs_diff": 0.0,
   "max_abs_value": 0.0,
   "ref_value": NaN,
   "cand_value": NaN,
   "ref_bits": "0xffea5349",
   "cand_bits": "0x7fffffff",
   "max_ulp": 0,
   "ndiff_nan_payload": 7171,
   "ndiff_value": 0,
   "nan_one_side_only": 0,
   "inf_one_side_only": 0
  }
 ],
 "ndiff_total": 7171,
 "n_total": 7569920,
 "ndiff_value_total": 0,
 "ndiff_nan_payload_total": 7171,
 "ndiff_ref_unwritten": 0,
 "ndiff_cand_unwritten": 0,
 "max_abs_diff": 0.0,
 "max_abs_value": 0.0,
 "max_ulp": 0,
 "nan_one_side_only": 0,
 "inf_one_side_only": 0
}
```

`ndiff/total` is over every byte of every pointer argument, so a large `total`
just means the buffers are large. `max_ulp` is the biggest gap between the two
results measured in units in the last place. `ndiff_ref_unwritten` /
`ndiff_cand_unwritten` count elements that still hold the input sentinel on that
side -- i.e. that side never wrote them.

## Numerics-change context (a label, not a verdict)

The culprit pass is classified `layout` by `fz/passes.py`. Reduction or dot
present in the reference IR: `True`. A `numerics` class pass
(`accelerate-matmul`, `F32DotTC`, `triton-combine`, `optimize-thread-locality`)
is *expected* to change FP results, and a layout-family pass on a kernel with a
reduction can legitimately change the summation order. Neither is a reason to
drop the finding; both are recorded so a reviewer can filter with all results in
hand. The small-whole-number control above is the cheap discriminator: if the
difference persists there, reassociation of an fp32 sum does not explain it.

## SMT reachability analysis

TODO: written by a reviewing agent

<!-- Everything below is mechanically collected raw material for that agent.
     Do not treat any of it as an analysis. -->

### Raw material

**Ops in the IR diff**

* only in the output IR: `ttg.blocked`
* only in the input IR: (none)
* all ops touched by the diff: `arith.addf`, `arith.addi`, `arith.andi`, `arith.cmpf`, `arith.muli`, `arith.negf`, `arith.ori`, `arith.select`, `math.absf`, `tt.addptr`, `tt.broadcast`, `tt.expand_dims`, `tt.extern_elementwise`, `tt.load`, `tt.ptr`, `tt.reduce`, `tt.reduce.return`, `tt.splat`, `tt.store`, `ttg.blocked`, `ttg.convert_layout`, `ttg.slice`

**Layout attributes in the IR diff**

* only in the output IR: `blocked5`, `blocked6`, `ttg.blocked`
* only in the input IR: (none)

**Diff size**: +83 / -64 lines.

**Structure present in either IR**: dot = `False`,
reduce/scan = `True`, loop = `False`.

**Concurrency constructs present in either IR**:
(none)

**Cross-instance constructs present in either IR**:
(none)

**Launch grid**: `(13, 1, 1)` (programs = 13). A grid of
one program means a single instance is enough to show the difference.

**Determinism measured**: the same build run 3x on one input is bit-identical
(pre-screen, and again at this exact shape -- see the self-consistency block in
the report); the mismatch reproduced 3/3 on re-run and
`mismatch` over 18 further inputs, fill modes cycling
through randn, uniform, extreme, intval, zeros, smallint, denorm, randn2, extreme2, onesish, pow2, fp16edge, int24, signedzero, nanpayload, denorm16, tiny, wide.

**Hopper-only tokens in the diff**: (none)
**Blackwell-only tokens in the diff**: (none)

**First 60 diff lines**

```mlir
@@ -5,0 +6,2 @@
+#blocked5 = #ttg.blocked<{sizePerThread = [2, 1], threadsPerWarp = [4, 8], warpsPerCTA = [1, 32], order = [0, 1]}>
+#blocked6 = #ttg.blocked<{sizePerThread = [1, 1], threadsPerWarp = [8, 4], warpsPerCTA = [1, 32], order = [0, 1]}>
@@ -44,52 +46,66 @@
-    %29 = tt.load %24, %28, %cst_0 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %30 = arith.muli %14, %cst_1 : tensor<1x256xi32, #blocked>
-    %31 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %32 = ttg.convert_layout %31 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %33 = tt.broadcast %30 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %34 = arith.addi %32, %33 : tensor<8x256xi32, #blocked>
-    %35 = tt.splat %arg1 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %36 = tt.addptr %35, %34 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %37 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %38 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %39 = ttg.convert_layout %38 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %40 = arith.andi %37, %39 : tensor<8x256xi1, #blocked>
-    %41 = tt.load %36, %40, %cst_0 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %42 = arith.negf %29 : tensor<8x256xf32, #blocked>
-    %43 = tt.extern_elementwise %41 {libname = "", libpath = "", pure = true, symbol = "__nv_tanhf"} : (tensor<8x256xf32, #blocked>) -> tensor<8x256xf32, #blocked>
-    %44 = math.absf %43 : tensor<8x256xf32, #blocked>
-    %45 = arith.addf %44, %cst : tensor<8x256xf32, #blocked>
-    %46 = tt.extern_elementwise %42, %45 {libname = "", libpath = "", pure = true, symbol = "__nv_fmodf"} : (tensor<8x256xf32, #blocked>, tensor<8x256xf32, #blocked>) -> tensor<8x256xf32, #blocked>
-    %47 = arith.cmpf ogt, %cst_3, %43 : tensor<8x256xf32, #blocked>
-    %48 = arith.cmpf une, %cst_3, %cst_3 : tensor<8x256xf32, #blocked>
-    %49 = arith.ori %47, %48 : tensor<8x256xi1, #blocked>
-    %50 = arith.select %49, %cst_3, %43 : tensor<8x256xi1, #blocked>, tensor<8x256xf32, #blocked>
-    %51 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %52 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %53 = ttg.convert_layout %52 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %54 = arith.andi %51, %53 : tensor<8x256xi1, #blocked>
-    %55 = arith.select %54, %50, %cst_3 : tensor<8x256xi1, #blocked>, tensor<8x256xf32, #blocked>
-    %56 = arith.muli %13, %cst_1 : tensor<1x256xi32, #blocked>
-    %57 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %58 = ttg.convert_layout %57 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %59 = tt.broadcast %56 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %60 = arith.addi %58, %59 : tensor<8x256xi32, #blocked>
-    %61 = tt.splat %arg2 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %62 = tt.addptr %61, %60 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %63 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %64 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %65 = ttg.convert_layout %64 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %66 = arith.andi %63, %65 : tensor<8x256xi1, #blocked>
-    tt.store %62, %42, %66 : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %67 = arith.muli %12, %cst_1 : tensor<1x256xi32, #blocked>
-    %68 = tt.broadcast %7 : tensor<8x1xi32, #blocked1> -> tensor<8x256xi32, #blocked1>
-    %69 = ttg.convert_layout %68 : tensor<8x256xi32, #blocked1> -> tensor<8x256xi32, #blocked>
-    %70 = tt.broadcast %67 : tensor<1x256xi32, #blocked> -> tensor<8x256xi32, #blocked>
-    %71 = arith.addi %69, %70 : tensor<8x256xi32, #blocked>
-    %72 = tt.splat %arg3 : !tt.ptr<f32> -> tensor<8x256x!tt.ptr<f32>, #blocked>
-    %73 = tt.addptr %72, %71 : tensor<8x256x!tt.ptr<f32>, #blocked>, tensor<8x256xi32, #blocked>
-    %74 = tt.broadcast %17 : tensor<1x256xi1, #blocked> -> tensor<8x256xi1, #blocked>
-    %75 = tt.broadcast %8 : tensor<8x1xi1, #blocked1> -> tensor<8x256xi1, #blocked1>
-    %76 = ttg.convert_layout %75 : tensor<8x256xi1, #blocked1> -> tensor<8x256xi1, #blocked>
-    %77 = arith.andi %74, %76 : tensor<8x256xi1, #blocked>
-    tt.store %73, %46, %77 : tensor<8x256x!tt.ptr<f32>, #blocked>
-    %78 = "tt.reduce"(%55) <{axis = 1 : i32}> ({
+    %29 = ttg.convert_layout %24 : tensor<8x256x!tt.ptr<f32>, #blocked> -> tensor<8x256x!tt.ptr<f32>, #blocked5>
+    %30 = ttg.convert_layout %28 : tensor<8x256xi1, #blocked> -> tensor<8x256xi1, #blocked5>
+    %31 = ttg.convert_layout %cst_0 : tensor<8x256xf32, #blocked> -> tensor<8x256xf32, #blocked5>
+    %32 = tt.load %29, %30, %31 evictionPolicy = evict_first : tensor<8x256x!tt.ptr<f32>, #blocked5>
```

