# The exact wheel set the numbers in this artifact were taken with.
#
# This is `.venv/bin/pip freeze` on that machine, with two lines changed and one dropped.
# See "Level 3" in README.md for how the virtualenv was made and what pip cannot give you.
#
# DROPPED: the editable Triton line. `pip freeze` reports Triton as
#     -e git+ssh://git@github.com/Youngzt998/triton.git@<commit>#egg=triton
# which a reviewer cannot resolve, and should not: Triton has to be BUILT FROM THIS CHECKOUT,
# not installed from anywhere. `pip install -e .` from the repository root, after the LLVM
# build. A released Triton wheel will not work -- the artifact uses
# `reduction_ordering=inner_tree` and the `tritongpu-optimize-reduction-layout` pass, which
# exist only in this fork.
#
# CHANGED: `torch==2.12.0` is what pip freeze prints; `torch.__version__` is `2.12.0+cu130`,
# and the CUDA 13.0 build is the one that was used. Plain `pip install torch==2.12.0` may give
# you a different CUDA build. Check `torch.version.cuda` reads 13.0 after installing.
#
# CHANGED: pyptx was installed from a local aarch64 wheel, which pip freeze records as
#     pyptx @ file:///tmp/ae/wheels/pyptx-0.1.1-cp312-cp312-manylinux_2_24_aarch64.manylinux_2_28_aarch64.whl
# A path on that machine is no use to you, so the pin below is the plain version. pyptx is
# needed by `bitequiv/ptx_reduction.py` and therefore by the `checker.corpus` step; no other
# step imports it.
#
# NOT IN HERE: cuBLASLt. It is not a wheel. `bitequiv/cublas_match/ltapi.py` globs
# /usr/local/cuda*/lib64 and /usr/local/cuda*/targets/*/lib and takes the newest by file name.
# On this box that is 13.2.2.2 under /usr/local/cuda-13.1/, which is the 13.2.2 every row of
# data/env.csv records. The `nvidia-cublas==13.1.1.3` wheel below is present but is not what
# gets loaded. Which cuBLASLt you load changes the bits -- see "Machine and versions".
#
# NOT IN HERE: LLVM. Build-time only, and not a Python package. See Level 3.

Arpeggio==2.0.3
autopep8==2.3.2
caliper-reader==0.4.1
cmake==3.31.10
contourpy==1.3.3
cuda-bindings==13.3.1
cuda-pathfinder==1.5.5
cuda-toolkit==13.0.2
cycler==0.12.1
dill==0.4.1
execnet==2.1.2
expecttest==0.3.0
filelock==3.29.1
fonttools==4.63.0
fsspec==2026.4.0
iniconfig==2.3.0
isort==8.0.1
Jinja2==3.1.6
kiwisolver==1.5.0
lit==18.1.8
llnl-hatchet==2026.1.0
MarkupSafe==3.0.3
matplotlib==3.10.9
mpmath==1.3.0
msgpack==1.1.2
multiprocess==0.70.19
networkx==3.6.1
ninja==1.13.0
numpy==2.4.6
nvidia-cublas==13.1.1.3
nvidia-cuda-cupti==13.0.85
nvidia-cuda-nvrtc==13.0.88
nvidia-cuda-runtime==13.0.96
nvidia-cudnn-cu13==9.20.0.48
nvidia-cufft==12.0.0.61
nvidia-cufile==1.15.1.6
nvidia-curand==10.4.0.35
nvidia-cusolver==12.0.4.66
nvidia-cusparse==12.6.3.3
nvidia-cusparselt-cu13==0.8.1
nvidia-nccl-cu13==2.29.7
nvidia-nvjitlink==13.0.88
nvidia-nvshmem-cu13==3.4.5
nvidia-nvtx==13.0.85
packaging==26.2
pandas==2.3.3
pillow==12.2.0
platformdirs==4.10.0
pluggy==1.6.0
py==1.11.0
pybind11==3.0.4
pycodestyle==2.14.0
pydot==4.0.1
Pygments==2.20.0
pyparsing==3.3.2
pyptx==0.1.1
pytest==9.0.3
pytest-forked==1.6.0
pytest-xdist==3.8.0
python-dateutil==2.9.0.post0
pytz==2026.2
PyYAML==6.0.3
ruff==0.9.1
scipy==1.17.1
setuptools==75.8.0
six==1.17.0
sympy==1.14.0
textX==4.3.0
torch==2.12.0
typing_extensions==4.15.0
tzdata==2026.2
wheel==0.47.0
yapf==0.43.0
