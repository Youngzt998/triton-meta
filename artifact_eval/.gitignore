cache/
*.ptx
*.jsonl

fusion/*.jsonl
fusion_oracle/*.jsonl
__pycache__/
fusion_real/*.jsonl
fusion_moe/*.jsonl
*/cache/
# The per-row tables are regenerated by `artifact.py --export`; only the summary and the
# machine log ship. Written as a sweep plus two exceptions so a new step's table is covered
# without editing this file.
data/*.csv
!data/summary.csv
!data/env.csv
